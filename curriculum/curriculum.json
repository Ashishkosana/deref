{
  "version": 1,
  "acts": [
    {
      "id": "foundations",
      "order": 0,
      "title": "Act 0 - Foundations",
      "subtitle": "How data is stored, read, and measured",
      "chapters": [
        "foundations"
      ]
    },
    {
      "id": "linear",
      "order": 1,
      "title": "Act I - Linear",
      "subtitle": "Tapes, windows, piles, halving",
      "chapters": [
        "arrays-hashing",
        "two-pointers",
        "sliding-window",
        "stack",
        "binary-search"
      ]
    },
    {
      "id": "linked-recursive",
      "order": 2,
      "title": "Act II - Linked & Recursive",
      "subtitle": "Nodes, self-reference, trees, heaps",
      "chapters": [
        "linked-list",
        "recursion",
        "trees",
        "heaps"
      ]
    },
    {
      "id": "search-optimize",
      "order": 3,
      "title": "Act III - Search & Optimize",
      "subtitle": "Explore, prune, memoize, commit",
      "chapters": [
        "backtracking",
        "graphs",
        "tries",
        "greedy",
        "intervals",
        "dynamic-programming"
      ]
    }
  ],
  "chapters": [
    {
      "id": "foundations",
      "actId": "foundations",
      "order": 1,
      "title": "Foundations: The Tape, the Robot, the Cost",
      "oneLineGoal": "Understand arrays, indices, pointers, cost, and hashing from zero - no problems, just poke and watch.",
      "mechanism": "Everything downstream is a tape of crates read by pointer-robots; a step is one unit of work.",
      "prereqChapters": [],
      "bossCombinesWith": [],
      "unlockRule": "free",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "foundations.l1",
          "order": 1,
          "name": "Arrays & Indices",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "An array is a fixed row of boxes sitting side by side, and each box holds one value. Every box also has a position number, called its index, that tells you where the box sits in the row \u2014 and if you know a box's number, you can grab what's inside it instantly.",
          "teaches": "A fixed row of numbered crates; reading one by its index is instant.",
          "engineLevelId": "foundations/arrays-and-indices",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "foundations.l2",
          "order": 2,
          "name": "Pointers",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "A pointer (also called a cursor) is just a bookmark: a little marker that remembers WHICH slot you're currently looking at. It doesn't hold the data itself \u2014 it holds a position, and that position is just a number.",
          "teaches": "A pointer is just a stored number that marks a position; moving it is i = i + 1.",
          "engineLevelId": "foundations/pointers-cursors",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "foundations.l3",
          "order": 3,
          "name": "Walking the Tape",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "One robot visits every crate once; the STEPS meter counts to N. Grounds step / work / N.",
          "teaches": "One robot visits every crate once; the STEPS meter counts to N. Grounds step / work / N.",
          "engineLevelId": "foundations/walking-the-tape",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "foundations.l4",
          "order": 4,
          "name": "Fast vs Slow (Big-O)",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "Big-O is a way of describing how the AMOUNT OF WORK a program does grows as the pile of data it handles gets bigger. It is not a stopwatch measuring seconds \u2014 it is the shape of the growth: does the work rise gently, in step with the pile (\"fast\"), or blow up much faster than the pile (\"slow\")?",
          "teaches": "Touch each thing once (O(n)) vs check every pair (O(n^2)); why it matters only when N is big.",
          "engineLevelId": "foundations/fast-vs-slow-bigo",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "foundations.l5",
          "order": 5,
          "name": "Why Sorting Helps",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "Sorting just means putting the crates in order, smallest on the left, largest on the right. Once they're in order, looking at ONE crate quietly tells you something about ALL the others: everything to its left is smaller, everything to its right is bigger. So a single glance can let you throw away a whole chunk of the tape without ever checking it \u2014 and that's the thing that makes fast searching possible.",
          "teaches": "On sorted data one comparison rules out a whole region.",
          "engineLevelId": "foundations/why-sorting-helps",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "foundations.l6",
          "order": 6,
          "name": "Hashing",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "A hash map (Python's `dict`) or hash set (Python's `set`) is a big wall of labelled slots plus a tiny fixed rule that turns any value into the exact slot where it belongs. So instead of looking through everything to answer \"is X in here?\", you compute the one slot X would live in and jump straight to it.",
          "teaches": "A wall of labelled mailboxes: 'have I seen X?' is instant instead of a re-scan; pay memory, buy speed.",
          "engineLevelId": "foundations/hashing-o1-lookup",
          "targetComplexity": null,
          "passRequires": "viewed"
        }
      ]
    },
    {
      "id": "arrays-hashing",
      "actId": "linear",
      "order": 1,
      "title": "Arrays & Hashing: The Mailbox Wall",
      "oneLineGoal": "Learn to trade a little memory for huge speed by remembering what you've seen in a hash set/map, turning O(n^2) tape-walks into single-pass O(n) lookups.",
      "mechanism": "A hash map is a wall of instantly-addressable mailboxes: by remembering what you've already seen (or its count, or its index) you replace a robot's slow walk back over the whole crate-tape with a single teleport lookup, trading memory for speed.",
      "prereqChapters": [
        "foundations"
      ],
      "bossCombinesWith": [
        "foundations (sorting): Longest Consecutive Sequence's naive answer is the sort-then-scan reflex from Foundations (O(n log n)) \u2014 the boss fuses in the hash set to recognize and beat it at O(n)."
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "arrays-hashing.l1",
          "order": 1,
          "name": "The Mailbox Wall",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "No-fail sandbox: a tape of numbered crates sits beside an empty wall of labeled mailboxes. Poke a crate to send the robot to check 'have I seen this value?' the slow way \u2014 it walks back over every crate while the op-meter climbs. Then drop the value's token into its labeled mailbox and re-check: the robot now teleports straight to that mailbox in one step. Toggle both ways and watch the op-meter and the growing wall.",
          "teaches": "The core trade behind the whole family: a hash set/map gives O(1) 'have I seen it / what's its count / where is it' lookups versus an O(n) linear scan of the tape. Introduces keys, membership, and the space-for-time idea from zero.",
          "engineLevelId": "arrays-hashing/the-mailbox-wall",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "arrays-hashing.l2",
          "order": 2,
          "name": "Contains Duplicate",
          "kind": "tutorial",
          "difficulty": 1,
          "problemText": "Contains Duplicate: given the crate-tape of numbers, return True if any value appears at least twice. Watch the tracer run the solution \u2014 the robot walks the tape once dropping each value into its mailbox; the instant it reaches a mailbox that already holds a token, it flags the duplicate and stops. Scrub back and forth to see exactly when the set 'catches' the repeat.",
          "teaches": "The 'seen set' pattern: one pass, check-then-add membership in a hash set. Contrast the O(n) time / O(n) space solution against the O(n^2) compare-every-pair walk the meter would rack up.",
          "engineLevelId": "arrays-hashing/contains-duplicate",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "arrays-hashing.l3",
          "order": 3,
          "name": "Valid Anagram",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Valid Anagram: decide if string t is a rearrangement of string s. The scaffold builds a count-mailbox per character, walks s adding counts, then walks t. One line is missing \u2014 fill in the line that decrements each character's count for t (and detects a missing/negative key) so that any leftover non-zero count exposes a mismatch.",
          "teaches": "Frequency counting with a hash map (dict / Counter): keys are characters, values are tallies, and equal multisets mean all counts return to zero. Moves beyond membership to using map values.",
          "engineLevelId": "arrays-hashing/valid-anagram",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "arrays-hashing.l4",
          "order": 4,
          "name": "Two Sum",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Two Sum: given the tape and a target, return the indices of the two crates that sum to target. The scaffold walks the tape storing each value->index in the mailbox wall. One line is missing: fill in the check that asks 'is (target - current) already in a mailbox?' and, if so, returns the stored index alongside the current one.",
          "teaches": "The complement trick \u2014 the single most important hashing move: store value->index as you scan and look up the needed partner in O(1), collapsing the O(n^2) nested-loop search into one pass. This is the chapter's 'aha'.",
          "engineLevelId": "arrays-hashing/two-sum",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "arrays-hashing.l5",
          "order": 5,
          "name": "Two Sum, No Scaffold",
          "kind": "open",
          "difficulty": 2,
          "problemText": "Rewrite Two Sum from a blank editor: one pass over the tape, a value->index mailbox wall, return the two indices. Nothing is pre-filled \u2014 the tracer animates whatever you type and the op-meter shows your pass count so you can confirm it stays single-pass.",
          "teaches": "Consolidate the complement pattern by producing it cold; internalize the 'build the map while you scan, look up as you go' loop shape you will reuse across the entire family.",
          "engineLevelId": "arrays-hashing/two-sum-no-scaffold",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "arrays-hashing.l6",
          "order": 6,
          "name": "Group Anagrams",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Group Anagrams: cluster a list of strings so all mutual anagrams share a group. Write it cold: for each word compute a canonical key (a 26-length character-count tuple, or the sorted letters) and use the mailbox wall to map that key to a bucket-list you append the word into; return the buckets.",
          "teaches": "Using a computed key as the mailbox label and mapping key -> list of items (a group-by / bucket). Extends frequency counting into a canonical-form signature and runs in O(n*k) instead of comparing every pair.",
          "engineLevelId": "arrays-hashing/group-anagrams",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "arrays-hashing.l7",
          "order": 7,
          "name": "Encode and Decode Strings",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Encode and Decode Strings: implement encode(list[str]) -> str and decode(str) -> list[str] that survive any characters, including delimiters. Write both: prefix each string with its length and a separator (e.g. '5#hello'); on decode the robot reads the number, hops exactly that many crates forward, and slices out each string.",
          "teaches": "Length-prefix serialization and disciplined index arithmetic \u2014 walking a pointer forward by a computed offset on the tape. Reinforces Foundations indexing and shows that not every array problem needs a hash map.",
          "engineLevelId": "arrays-hashing/encode-and-decode-strings",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "arrays-hashing.l8",
          "order": 8,
          "name": "Valid Sudoku",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Valid Sudoku: given a 9x9 board, decide whether the filled cells break any Sudoku rule. Write it: keep one hash set per row, per column, and per 3x3 box (box key = (row//3, col//3)); scan every cell once and flag if a digit is already present in its row-, column-, or box-set.",
          "teaches": "Running several hash sets in parallel and deriving a bucket key from grid coordinates via (row//3, col//3). One O(cells) pass replaces re-scanning each unit, generalizing the 'seen set' onto a 2-D tape.",
          "engineLevelId": "arrays-hashing/valid-sudoku",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "arrays-hashing.l9",
          "order": 9,
          "name": "Product of Array Except Self",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Product of Array Except Self: return an array where answer[i] is the product of every crate except i \u2014 with no division allowed, and the brute-force O(n^2) 'multiply all others for each i' trips the brown-out. Beat it: one left-to-right pass filling prefix products, one right-to-left pass multiplying in suffix products, using only the output tape as scratch.",
          "teaches": "Prefix/suffix accumulation across the tape in two O(n) passes with O(1) extra space \u2014 a pure array technique (no hashing) showing how staged directional passes replace nested loops.",
          "engineLevelId": "arrays-hashing/product-of-array-except-self",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "arrays-hashing.l10",
          "order": 10,
          "name": "Top K Frequent Elements",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Top K Frequent Elements: return the k most frequent values on the tape. A sort-by-frequency solution is O(n log n) and drags the power meter toward brown-out. Hit the O(n) target: build a frequency map, then a bucket array indexed BY frequency (index = count), and read buckets from the highest count downward until you have k values.",
          "teaches": "Combining a frequency hash map with bucket sort \u2014 using the count itself as an array index \u2014 to reach O(n) time and dodge the comparison-sort log factor. Sets up the sort-vs-hash judgment the boss demands.",
          "engineLevelId": "arrays-hashing/top-k-frequent-elements",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "arrays-hashing.l11",
          "order": 11,
          "name": "Longest Consecutive Sequence",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "BOSS \u2014 Longest Consecutive Sequence: find the length of the longest run of consecutive integers on the tape in O(n). The Foundations reflex is to sort then scan (O(n log n)) \u2014 that trips the brown-out. Fuse it with hashing: dump every value into a set, and start counting a run ONLY from a value whose predecessor (n-1) is absent, walking n+1, n+2, ... upward. Every value is visited at most twice.",
          "teaches": "Boss synthesis: recognizing when a hash set beats the sorting reflex, plus the 'only expand from a sequence start' insight that keeps total work O(n) despite the inner while-loop. Combines Foundations sorting intuition with everything learned about sets.",
          "engineLevelId": "arrays-hashing/longest-consecutive-sequence",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "two-pointers",
      "actId": "linear",
      "order": 2,
      "title": "Two Pointers: The Rendezvous",
      "oneLineGoal": "Master the converging two-pointer walk on ordered data \u2014 collapse O(n^2) pair searches into a single O(n), O(1)-space pass, and know exactly when sortedness makes that legal.",
      "mechanism": "When a sequence is ordered, two pointer-robots starting at opposite ends can use a single end-to-end comparison to prove that a whole block of candidate pairs is hopeless and discard it, converging on the answer in one linear pass instead of checking every pair.",
      "prereqChapters": [
        "foundations",
        "arrays-hashing"
      ],
      "bossCombinesWith": [
        "sorting (from the Foundations act) \u2014 3Sum only becomes a two-pointer problem once you SORT to manufacture the monotonic order the converging pointers depend on, then wrap the Rendezvous walk inside an anchor loop with de-duplication."
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "two-pointers.l1",
          "order": 1,
          "name": "The Mirror Walk",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "No-fail sandbox on the mechanic behind Valid Palindrome (LC 125): two pointer-robots stand on the ends of a numbered crate tape. Poke them to step inward one crate at a time and watch them compare the two mirrored crates each step, until they meet in the middle. Nothing to write, nothing to fail.",
          "teaches": "The converging-two-pointers substrate from zero: L begins at the left, R at the right, the `while L < R` meeting invariant, that one comparison inspects an end-pair, and how to read the op meter and drag the scrubber.",
          "engineLevelId": "two-pointers/the-mirror-walk",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "two-pointers.l2",
          "order": 2,
          "name": "The Rendezvous",
          "kind": "tutorial",
          "difficulty": 2,
          "problemText": "Two Sum II \u2014 Input Array Is Sorted (LC 167). Watch the baked reference solution walk the sorted tape: it sums the two ends, pulls R inward when the sum overshoots `target` and pushes L outward when it falls short. Then replay the O(n^2) brute-force twin and watch the power meter brown out red halfway across, and the hash-map twin pass on time but light the space gauge.",
          "teaches": "Why sortedness lets a single end-comparison throw away a whole row/column of the pair matrix, collapsing O(n^2) to O(n); and how to read the brown-out power meter (time) versus the space gauge (memory) to feel the two costs the two-pointer answer avoids.",
          "engineLevelId": "two-pointers/the-rendezvous",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "two-pointers.l3",
          "order": 3,
          "name": "Which Way to Step?",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Two Sum II again, with the whole loop written for you but exactly ONE line blanked \u2014 the decision that moves a pointer (`if s > target: R -= 1  else: L += 1`). Fill the single missing line so the robots converge onto the pair.",
          "teaches": "Converting the watched mechanic into code: the monotonic decision rule that chooses which pointer moves, and the proof-by-elimination that moving the OTHER pointer could never reach the target from here.",
          "engineLevelId": "two-pointers/which-way-to-step",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "two-pointers.l4",
          "order": 4,
          "name": "Palindrome Checkpoint",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Valid Palindrome (LC 125), written cold in a blank editor: converge two pointers from both ends, skip crates that aren't alphanumeric, compare the lowercased characters, and return False on the first mismatch.",
          "teaches": "Cold-writing a converging loop with an equality test and an early exit; and how to advance a single pointer mid-loop (an in-place skip) without ever violating the `L < R` invariant.",
          "engineLevelId": "two-pointers/palindrome-checkpoint",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "two-pointers.l5",
          "order": 5,
          "name": "Rendezvous, From Memory",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Two Sum II (LC 167) with no scaffold at all \u2014 the chapter's transfer test. Cold-write the converging two-pointer solution and state its cost: O(n) time, O(1) space. The grader's correctness + complexity gates must both go green with zero aux space on the gauge.",
          "teaches": "Full independent recall of the target-sum two-pointer pattern and naming its complexity \u2014 the go/no-go check that the mechanic actually transferred, not just that you can follow a walkthrough.",
          "engineLevelId": "two-pointers/rendezvous-from-memory",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "two-pointers.l6",
          "order": 6,
          "name": "Widest Cistern",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Container With Most Water (LC 11). The obvious check-every-pair solution browns out at O(n^2); reach the O(n) target by converging from both ends and always advancing the pointer standing at the SHORTER wall.",
          "teaches": "The greedy converging invariant: because area is bounded by the shorter wall, moving the taller-wall pointer can never increase it \u2014 so the shorter-wall pointer is the only move that can improve, a rule you discover by watching the brute version run out of power.",
          "engineLevelId": "two-pointers/widest-cistern",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "two-pointers.l7",
          "order": 7,
          "name": "After the Storm",
          "kind": "optimize",
          "difficulty": 5,
          "problemText": "Trapping Rain Water (LC 42). A correct precompute-left-max / right-max solution passes correctness and time but lights the space gauge at O(n); hit O(1) space by converging two pointers that each carry a running max and always advance whichever side has the smaller running max.",
          "teaches": "Optimizing SPACE, not just time: trading two prefix/suffix arrays for two running-max scalars, and the invariant that the smaller running max alone bounds the water trapped above the current pointer.",
          "engineLevelId": "two-pointers/after-the-storm",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "two-pointers.l8",
          "order": 8,
          "name": "The Zero Sum",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "3Sum (LC 15): return every unique triplet that sums to zero. SORT the tape first, fix one anchor crate in an outer loop, then run the Rendezvous two-pointer walk across the remaining crates \u2014 skipping duplicate values at the anchor and at both pointers so no triplet repeats.",
          "teaches": "Composing two-pointers with sorting: sort to create the monotonic order the pointers require, reduce each anchor to an inner Two-Sum-II Rendezvous, and de-duplicate on all three indices \u2014 O(n^2) time with O(1) extra space, fusing this chapter with sorting from Foundations.",
          "engineLevelId": "two-pointers/the-zero-sum",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "sliding-window",
      "actId": "linear",
      "order": 3,
      "title": "Sliding Window: The Moving Frame",
      "oneLineGoal": "Sweep a single-pass moving window over a linear structure \u2014 fixed-size or constraint-driven \u2014 to answer \"best / longest / smallest contiguous run\" questions in O(n).",
      "mechanism": "Instead of re-scanning every subarray, keep a moving window over the tape and update a running summary as one edge enters and the other leaves \u2014 collapsing O(n\u00b7k)/O(n^2) rescans into a single O(n) pass, always expanding the right edge and shrinking the left only when a constraint demands it.",
      "prereqChapters": [
        "foundations",
        "arrays-hashing",
        "two-pointers"
      ],
      "bossCombinesWith": [
        "arrays-hashing (need/have frequency maps drive the shrink-to-minimize logic)"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "sliding-window.l1",
          "order": 1,
          "name": "The Window Frame",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "Maximum Sum Subarray of Size K (classic fixed-window warmup). No-fail poke-and-watch. A tape of numbered crates and a fixed k=3 window frame (two robot arms clamped k crates apart). First run the NAIVE mode: for every start index, re-add all k crates \u2014 watch the op-meter rack up ~n\u00b7k additions and the brown-out power meter sag. Then flip the SLIDE switch: as the frame steps right, one crate leaves the left arm (subtract it) and one enters the right (add it), so the running sum updates in 2 ops per step. Scrub back and forth to compare the two op-counts.",
          "teaches": "A window is a contiguous range on the tape, and sliding it right REUSES the previous sum instead of recomputing it \u2014 add the entering crate, subtract the leaving crate. This one move turns an O(n\u00b7k) rescan into a single O(n) pass; it is the whole chapter in miniature.",
          "engineLevelId": "sliding-window/the-window-frame",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "sliding-window.l2",
          "order": 2,
          "name": "Buy Low, Sell Right",
          "kind": "tutorial",
          "difficulty": 2,
          "problemText": "Best Time to Buy and Sell Stock (NeetCode, Easy). Watch it run. Tape of daily prices with a 'buy' robot (left edge) and a 'sell' robot (right edge). The animation advances the right robot one day at a time, tracking best profit = price[right] - price[left]; whenever a new lowest price appears under the right robot, the left 'buy' robot teleports forward to it. Step through with the scrubber and confirm the left robot NEVER moves backward.",
          "teaches": "The first VARIABLE window: the left edge is not fixed \u2014 it advances to a better starting point (the running minimum). One forward pass, carry a running best, left never backtracks. Establishes the 'left only moves forward' invariant that powers every variable window in the rest of the chapter.",
          "engineLevelId": "sliding-window/buy-low-sell-right",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "sliding-window.l3",
          "order": 3,
          "name": "No Repeats Allowed",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Longest Substring Without Repeating Characters (NeetCode, Medium). Fill the ONE missing line. Tape of characters; the window is the current substring of all-unique chars, tracked by a hash set drawn as a rack of lit tiles. The right robot advances and adds each char to the set; the answer is the longest window length seen. The blank you complete is the shrink step: while the incoming char is already in the set, remove the left char and advance the left robot. The op-meter proves each char enters and leaves the set at most once.",
          "teaches": "Variable window driven by a VALIDITY constraint (all-unique). Expand right every step; when the invariant breaks, shrink from the left until it holds again. A hash set gives O(1) membership so the full scan stays O(n) \u2014 this is arrays-hashing's set applied to a moving range.",
          "engineLevelId": "sliding-window/no-repeats-allowed",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "sliding-window.l4",
          "order": 4,
          "name": "Paint the Longest Streak",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Longest Repeating Character Replacement (NeetCode, Medium). Write it cold. Tape of letters; you may repaint up to k crates to any letter. Find the longest window you can make all-identical using at most k repaints. Maintain a frequency map of the letters currently in the window plus maxFreq (count of the most-common letter). The window is valid while (windowLength - maxFreq) <= k \u2014 the crates you would need to repaint fit within budget; when it isn't, advance the left robot. Track the longest valid window. Brown-out is generous here: correctness first.",
          "teaches": "A window governed by a COUNTING invariant rather than a set: valid when 'slots to fix' <= budget. Introduces a per-window aggregate (max frequency) as the shrink trigger, and reinforces expand-right / conditional-shrink-left on a numeric constraint.",
          "engineLevelId": "sliding-window/paint-the-longest-streak",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "sliding-window.l5",
          "order": 5,
          "name": "Two Baskets",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Fruit Into Baskets (canonical 'longest substring with at most K distinct' template, K=2; a common NeetCode sliding-window practice problem). Write it cold. A tape of fruit-type crates; you carry two baskets, each holding one type, and must pick from one contiguous run. Find the longest contiguous window containing at most 2 distinct types. Keep a freq map of types in the window; expand the right robot, and while the map holds more than 2 keys, shrink from the left (decrement the leftmost type, evict it when its count hits zero). Track the longest window.",
          "teaches": "The reusable 'at most K distinct' template: a freq map IS the window's contents, and you shrink while distinct-count exceeds the cap. This is the exact machinery the boss needs, rehearsed against a simpler constraint \u2014 consolidating the freq-map-shrink pattern before difficulty spikes.",
          "engineLevelId": "sliding-window/two-baskets",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "sliding-window.l6",
          "order": 6,
          "name": "The Rolling Match",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Permutation in String (NeetCode, Medium). Beat the brown-out. Given s1 and s2, decide whether s2 contains any permutation of s1 \u2014 a FIXED window of length len(s1) whose letter counts exactly equal s1's. The naive attempt rebuilds a 26-letter count for every window and compares all 26 each step; the power meter browns out on long strings (O(n\u00b726)). Optimize: hold one running count and a single 'matches' integer (how many of the 26 letter-counts currently equal the target). On each slide, update only the two affected letters (entering and leaving) and adjust 'matches' in O(1); answer yes when matches == 26. Hit the O(n) target to clear the brown-out.",
          "teaches": "Fixed-window frequency MATCHING, and the chapter's core lesson pushed to its limit: never recompute the aggregate \u2014 touch only what the entering/leaving crate changed, in O(1) per slide. Fuses the fixed window with arrays-hashing counts and turns O(n\u00b7k) into O(n).",
          "engineLevelId": "sliding-window/the-rolling-match",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "sliding-window.l7",
          "order": 7,
          "name": "Smallest Cover",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "Minimum Window Substring (NeetCode, Hard). Combine everything. Given strings s and t, find the SMALLEST window in s containing every character of t with multiplicity. Build a 'need' frequency map from t and a 'have' counter of how many required characters the current window satisfies. Expand the right robot, updating have; once the window is valid (have covers all of need), CONTRACT the left robot as far as possible while it stays valid, recording the smallest valid window seen. Each step is two-phase: grow to become valid, then shrink to stay minimal. Brown-out enforces a single O(n) pass \u2014 each robot crosses the tape exactly once.",
          "teaches": "The full variable-window boss: simultaneous expand-to-satisfy and shrink-to-minimize against a multiplicity constraint, powered by need/have frequency maps. Fuses sliding-window control flow with arrays-hashing counting \u2014 the hardest linear-scan pattern and the template behind most 'smallest/longest window that satisfies X' interview questions.",
          "engineLevelId": "sliding-window/smallest-cover",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "stack",
      "actId": "linear",
      "order": 4,
      "title": "The Stack: Spring-Loaded Piles & Monotonic Walls",
      "oneLineGoal": "Go from \"what is a stack?\" to wielding the monotonic stack that answers next-greater/smaller and largest-rectangle in a single O(n) pass.",
      "mechanism": "A stack lets you touch only the top of a pile \u2014 push to defer the most recent thing, pop to resolve it \u2014 and that one last-in-first-out rule powers both nested-structure matching (brackets, expressions) and the monotonic stack that finds every element's nearest greater/smaller neighbor in one linear sweep.",
      "prereqChapters": [
        "foundations",
        "arrays-hashing",
        "two-pointers"
      ],
      "bossCombinesWith": [
        "two-pointers"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "stack.l1",
          "order": 1,
          "name": "The Spring Platform",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "No problem to solve \u2014 a guided, no-fail tour. A single spring-loaded platform holds a vertical pile of numbered crates. Poke PUSH to drop a crate on top, POP to lift the top crate off, PEEK to read the top label without removing it. Try to grab a crate from the middle and the game shows it is physically blocked. The op-meter ticks once per action.",
          "teaches": "LIFO from zero: you can only ever add to or remove from the TOP, so the last crate pushed is the first popped. push / pop / peek / is-empty and their O(1) cost. The physical intuition that a stack has exactly one open end.",
          "engineLevelId": "stack/the-spring-platform",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "stack.l2",
          "order": 2,
          "name": "Balanced Brackets",
          "kind": "tutorial",
          "difficulty": 1,
          "problemText": "Valid Parentheses (NeetCode: Valid Parentheses). Watch a robot scan \"([{}])\" left to right: on each opener it pushes the matching CLOSER onto the platform, and on each closer it pops the top and checks the match. Scrub the timeline to watch the pile grow then drain; the string is valid iff the platform is empty at the end.",
          "teaches": "A stack as short-term memory for 'the most recent thing still waiting to be closed.' Why LIFO fits nested structure: the innermost unclosed bracket is always the one on top.",
          "engineLevelId": "stack/balanced-brackets",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "stack.l3",
          "order": 3,
          "name": "Close the Brackets",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Valid Parentheses \u2014 you write one line. The scaffold pushes openers and drives the loop; you fill the closing-bracket case: pop the top and compare, returning False on mismatch, plus guard the empty-stack case when a closer arrives with nothing to match. Confirm the final 'stack must be empty' check.",
          "teaches": "The pop-and-compare condition and the two failure edges beginners miss \u2014 a closer arriving on an empty stack, and leftover openers at the end.",
          "engineLevelId": "stack/close-the-brackets",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "stack.l4",
          "order": 4,
          "name": "The Twin Platform",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Min Stack (NeetCode: Min Stack). Design a stack that also returns its minimum in O(1). The scaffold provides push/pop/top on the main platform; you fill getMin by maintaining a second parallel 'min platform' that pushes the running minimum beside each value and pops in lockstep.",
          "teaches": "Augmenting a stack with a parallel bookkeeping stack so an aggregate (the min) stays O(1); the invariant that the two platforms rise and fall together.",
          "engineLevelId": "stack/the-twin-platform",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "stack.l5",
          "order": 5,
          "name": "The Postfix Calculator",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Evaluate Reverse Polish Notation (NeetCode: Evaluate Reverse Polish Notation). Write it cold: scan tokens, push numbers as crates, and on an operator pop the top two operands, apply it, and push the result back. The final lone crate is the answer.",
          "teaches": "A stack as an evaluation engine: operator arity (pop exactly two) and operand ORDER \u2014 for '-' and '/' the second crate popped is the left operand.",
          "engineLevelId": "stack/the-postfix-calculator",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "stack.l6",
          "order": 6,
          "name": "The Monotonic Wall",
          "kind": "primer",
          "difficulty": 2,
          "problemText": "A no-fail tour of a new discipline for the same platform. Feed in bars of varying heights one at a time; the rule forces you to POP every bar shorter than the incoming one before it lands (a decreasing wall). Poke bars in and watch a tall bar trigger a cascade of pops while the pile always stays sorted top-to-bottom. A counter shows each bar is pushed once and popped at most once.",
          "teaches": "The monotonic-stack invariant (the pile stays sorted) and the pop-WHILE loop that maintains it; the amortized insight that N pushes + at most N pops = O(N) total even though the code looks nested.",
          "engineLevelId": "stack/the-monotonic-wall",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "stack.l7",
          "order": 7,
          "name": "How Many Days Until Warmer?",
          "kind": "tutorial",
          "difficulty": 3,
          "problemText": "Daily Temperatures (NeetCode: Daily Temperatures). Watch the monotonic solution run: the platform holds the INDICES of days still waiting for a warmer one (a decreasing-temperature wall). Each new warmer day pops all colder waiting days and stamps each answer = today's index minus theirs. Scrub to watch waits resolve in bursts.",
          "teaches": "Storing indices (not values) so you can compute distances; the 'next greater element' pattern where each pop is the exact moment a waiting item finds its answer.",
          "engineLevelId": "stack/how-many-days-until-warmer",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "stack.l8",
          "order": 8,
          "name": "Resolve the Wait",
          "kind": "guided",
          "difficulty": 3,
          "problemText": "Daily Temperatures \u2014 fill the core lines: the while-loop that, for the current day, pops every now-resolved colder index and records its answer, then pushes the current index. The outer loop and result array are provided.",
          "teaches": "Translating the monotonic invariant into code: the while-pop-and-record step, and knowing to push the current index after resolving.",
          "engineLevelId": "stack/resolve-the-wait",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "stack.l9",
          "order": 9,
          "name": "Next Greater, Cold",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Daily Temperatures again, written cold with no scaffold \u2014 framed as the reusable 'next greater element' template (the NeetCode-150 next-greater problem). You decide what the stack holds, the pop condition, and how to leave still-unresolved days as 0.",
          "teaches": "Reproducing the next-greater-element template from scratch and recognizing it as a reusable stamp for a whole class of 'nearest bigger/smaller neighbor' problems.",
          "engineLevelId": "stack/next-greater-cold",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "stack.l10",
          "order": 10,
          "name": "Beat the Brown-Out: Car Fleet",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Car Fleet (NeetCode: Car Fleet). The tempting move \u2014 simulate every car's position over time \u2014 browns out the power meter on large inputs. Beat the meter: sort cars by start position (descending toward the target), convert each to arrival TIME, then make one monotonic pass \u2014 a car that would arrive no later than the fleet ahead is absorbed, otherwise it starts a new fleet. Count fleets while staying under the op budget.",
          "teaches": "Knowing when to STOP simulating and instead pre-sort + single-pass; fusing sorting (foundations) with a monotonic stack, where each surviving stack entry is one fleet; hitting O(n log n) to clear the brown-out.",
          "engineLevelId": "stack/beat-the-brown-out-car-fleet",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "stack.l11",
          "order": 11,
          "name": "The Skyline Vault",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "Largest Rectangle in Histogram (NeetCode: Largest Rectangle in Histogram). The obvious expand-each-bar approach is O(n\u00b2) and browns out. Combine everything: an increasing monotonic stack of (index, height) finds, for each bar, its previous-smaller and next-smaller boundaries \u2014 the two-pointer span idea \u2014 and the biggest rectangle is height \u00d7 (right boundary \u2212 left boundary). Handle the end-of-array flush. Clear it in O(n).",
          "teaches": "The capstone monotonic stack: using the stack to compute, in one pass, the two-sided span (previous-smaller / next-smaller boundaries) that the two-pointers chapter framed by hand \u2014 then maximizing width \u00d7 height. Robust boundary and flush handling.",
          "engineLevelId": "stack/the-skyline-vault",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "binary-search",
      "actId": "linear",
      "order": 5,
      "title": "Binary Search: Halving the Search Space",
      "oneLineGoal": "Find things in O(log n) by repeatedly throwing away the half of the space that cannot contain the answer.",
      "mechanism": "Maintain a shrinking [lo, hi] window over a sorted (or otherwise monotonic) space and, at each step, test the midpoint against a predicate to discard the half that provably cannot hold the answer.",
      "prereqChapters": [
        "foundations",
        "arrays-hashing",
        "two-pointers"
      ],
      "bossCombinesWith": [
        "two-pointers"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "binary-search.l1",
          "order": 1,
          "name": "The Crate Hunt",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "Guided-tour on a sorted tape of numbered crates with a lo-robot, a hi-robot, and a mid marker between them. You poke the mid crate; it flips open to reveal its number and greys out the entire half that can no longer contain your target. No-fail: keep poking until one crate remains. A side-by-side meter counts your ~log2(n) pokes against the n pokes a left-to-right scan would cost.",
          "teaches": "The core mechanism with zero code: a search window [lo, hi], its midpoint, that each comparison discards half of what is left, and why the tape MUST be sorted for discarding a half to be a valid move.",
          "engineLevelId": "binary-search/the-crate-hunt",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "binary-search.l2",
          "order": 2,
          "name": "Search, On Rails",
          "kind": "tutorial",
          "difficulty": 1,
          "problemText": "Binary Search (LC 704): watch the finished algorithm run on a sorted array hunting for a target. Scrub the timeline to replay lo, hi, and mid moving on the tape while the active comparison line lights up in the code panel each step.",
          "teaches": "The canonical `while lo <= hi` loop, the three-way compare (found / go right / go left), and the loop invariant: if the target exists, it is always still inside [lo, hi].",
          "engineLevelId": "binary-search/search-on-rails",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "binary-search.l3",
          "order": 3,
          "name": "Mind the Boundary",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Binary Search (LC 704): the full skeleton is provided but the two pointer-update lines are blank. Fill in `lo = mid + 1` and `hi = mid - 1` correctly. A wrong guess like `lo = mid` triggers a visible infinite-loop / brown-out so the mistake is felt, not just marked wrong.",
          "teaches": "Why each boundary moves PAST mid rather than to mid \u2014 the +1 / -1 that guarantees the window strictly shrinks, preventing off-by-one bugs and non-termination.",
          "engineLevelId": "binary-search/mind-the-boundary",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "binary-search.l4",
          "order": 4,
          "name": "Cold Search",
          "kind": "open",
          "difficulty": 2,
          "problemText": "Binary Search (LC 704): write the entire function from an empty editor, returning the index of target or -1. The tracer animates whatever you write, so a bad loop condition or mid formula shows up as robots overshooting the tape.",
          "teaches": "Internalizing the whole template end to end without scaffolding: initialize lo/hi, the loop condition, computing mid, the compare, shrinking the window, and the return.",
          "engineLevelId": "binary-search/cold-search",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "binary-search.l5",
          "order": 5,
          "name": "Flatten the Grid",
          "kind": "guided",
          "difficulty": 3,
          "problemText": "Search a 2D Matrix (LC 74): the m\u00d7n grid of crates is drawn as a real 2D substrate, but the binary search runs over a virtual index range 0..m*n-1 shown as a ghost tape beneath it. Fill the one line that maps mid to a real cell: `row, col = mid // n, mid % n`.",
          "teaches": "The search space is an abstract range of positions, not the literal data \u2014 you can binary-search any monotonic index range as long as you can translate the midpoint into the structure you are probing.",
          "engineLevelId": "binary-search/flatten-the-grid",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "binary-search.l6",
          "order": 6,
          "name": "The Last One Before",
          "kind": "guided",
          "difficulty": 3,
          "problemText": "Time Based Key-Value Store (LC 981): a hashmap maps each key to its chronological (timestamp, value) list, drawn as separate tapes. `get(key, t)` must return the value at the largest timestamp <= t. Fill the branch that, on `arr[mid][0] <= t`, records the candidate and keeps pushing right for a possibly-later match.",
          "teaches": "Boundary search (predecessor / upper-bound): binary search that converges on the best-candidate-so-far instead of demanding an exact hit, and how it plugs into a hashmap of pre-sorted lists (why arrays-hashing is a prerequisite).",
          "engineLevelId": "binary-search/the-last-one-before",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "binary-search.l7",
          "order": 7,
          "name": "Where the Tape Snapped",
          "kind": "tutorial",
          "difficulty": 3,
          "problemText": "Find Minimum in Rotated Sorted Array (LC 153): watch a binary search that has NO target. It compares nums[mid] to nums[hi] to decide which side the rotation pivot fell on, and the animation highlights the still-sorted half in green versus the broken half in red as the window collapses onto the minimum.",
          "teaches": "The midpoint test can be any monotonic predicate ('is mid in the left, higher segment?'), not equality to a target \u2014 this is the conceptual bridge from 'search for a value' to 'search for a condition boundary'.",
          "engineLevelId": "binary-search/where-the-tape-snapped",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "binary-search.l8",
          "order": 8,
          "name": "Search the Broken Ring",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Search in Rotated Sorted Array (LC 33): write it cold. At each step decide which half is the sorted one (compare nums[lo] to nums[mid]), then range-check whether the target lies within that sorted half; discard the other half accordingly.",
          "teaches": "Composing predicate reasoning with a target search \u2014 identify the sorted half, test if the target fits inside its known range, and only then choose which side to keep. Directly builds on the pivot logic from level 7.",
          "engineLevelId": "binary-search/search-the-broken-ring",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "binary-search.l9",
          "order": 9,
          "name": "Beat the Clock, Koko",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Koko Eating Bananas (LC 875): you are handed a correct but naive solution that tries every eating speed 1..max(piles) and it browns out the power meter at O(n\u00b7max). Rewrite it to binary-search the eating speed and hit the O(n\u00b7log max) target to keep the power on.",
          "teaches": "Binary search on the ANSWER space: define a monotonic feasibility predicate (can she finish at speed k within h hours?) and search for the boundary where infeasible flips to feasible, instead of scanning every candidate answer. Fuses the abstract-range idea (L5) with predicate boundary search (L6/L7).",
          "engineLevelId": "binary-search/beat-the-clock-koko",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "binary-search.l10",
          "order": 10,
          "name": "Median of Two Tapes",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "Median of Two Sorted Arrays (LC 4): the brute force is the two-pointer merge you already mastered (walk both tapes to the middle, O(m+n)); the target is O(log(m+n)). Binary-search the partition point of the shorter tape so that the combined left halves hold exactly half the elements, adjusting the cut until max(left) <= min(right).",
          "teaches": "Fusing the two-pointer merge intuition with binary search on a partition boundary: instead of walking to the median, search for the cut that balances both arrays and satisfies the crossing invariant maxLeftA <= minRightB and maxLeftB <= minRightA.",
          "engineLevelId": "binary-search/median-of-two-tapes",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "linked-list",
      "actId": "linked-recursive",
      "order": 1,
      "title": "Linked Lists: The Cable Yard",
      "oneLineGoal": "Master pointer rewiring on linked lists \u2014 traverse, reverse, dummy-head building, and fast/slow \u2014 from a first hop all the way to designing an O(1) LRU cache.",
      "mechanism": "A linked list is a chain of crates joined by re-pluggable \"next\" cables that you can only walk one hop at a time, so you solve problems by advancing pointer-robots and rewiring cables \u2014 leaning on a dummy-head sentinel to erase edge cases and a fast/slow pair to find cycles and midpoints.",
      "prereqChapters": [
        "foundations",
        "two-pointers",
        "arrays-hashing"
      ],
      "bossCombinesWith": [
        "arrays-hashing"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "linked-list.l1",
          "order": 1,
          "name": "The Cable Yard",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "No coding, no fail state. A chain of crates is already wired up. Click a crate to reveal its .val and the .next cable leaving it; drive the head-robot hop-by-hop along cables until it lands on the ground (None) and stops. Try to teleport straight to crate #4 \u2014 it refuses, you must walk. Finally, re-plug one cable to splice a fresh crate in front of head (prepend) and watch the whole chain re-address.",
          "teaches": "The node substrate: a node is (val, next); head points at the first crate, None terminates the chain; access is sequential-only (no random index like an array); prepend/append is just re-plugging a cable.",
          "engineLevelId": "linked-list/the-cable-yard",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "linked-list.l2",
          "order": 2,
          "name": "Walk the Line",
          "kind": "tutorial",
          "difficulty": 1,
          "problemText": "Watch a finished solution run and scrub it: `curr = head; total = 0; while curr: total += curr.val; curr = curr.next`. The curr-robot hops cable to cable, the op-meter ticks once per hop, and the loop halts the instant curr steps onto None. Activity: read off the list's length and value-sum from the final frame.",
          "teaches": "The canonical traversal loop idiom (`curr = curr.next` until None), why one full pass is O(n), and how the while-condition terminates cleanly at the None terminator.",
          "engineLevelId": "linked-list/walk-the-line",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "linked-list.l3",
          "order": 3,
          "name": "Flip It \u2014 Watch the Dance",
          "kind": "tutorial",
          "difficulty": 2,
          "problemText": "Reverse Linked List (NeetCode). Watch the three-robot dance frame by frame: prev starts on None, curr on head; each step saves nxt = curr.next, re-plugs curr.next = prev, then slides prev and curr forward one crate. Scrub to see the cables flip one at a time until curr hits None and prev is standing on the new head.",
          "teaches": "In-place reversal via three pointers (prev/curr/nxt); crucially, why you must stash nxt BEFORE rewiring curr.next or you lose the rest of the chain.",
          "engineLevelId": "linked-list/flip-it-watch-the-dance",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "linked-list.l4",
          "order": 4,
          "name": "Flip It \u2014 Plug the Gap",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Reverse Linked List (NeetCode). The full loop scaffold is given with exactly one line blanked. Fill in the rewire line `curr.next = prev` and confirm the advance order. Get the order wrong and the tracer visibly strands the tail \u2014 half the chain vanishes into the void.",
          "teaches": "The rewire-before-advance ordering, and the single most common linked-list bug: dropping the remainder of the list by advancing before (or instead of) saving the next pointer.",
          "engineLevelId": "linked-list/flip-it-plug-the-gap",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "linked-list.l5",
          "order": 5,
          "name": "Flip It \u2014 Solo",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Reverse Linked List (NeetCode). Write the whole function cold from an empty body: given head, return the head of the reversed list. Your first fully solo rewire, on the pattern you just watched and completed.",
          "teaches": "Reproducing the reversal pattern unaided; owning the prev/curr/nxt bookkeeping and the O(n)-time / O(1)-space in-place mutation.",
          "engineLevelId": "linked-list/flip-it-solo",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "linked-list.l6",
          "order": 6,
          "name": "The Zipper",
          "kind": "guided",
          "difficulty": 3,
          "problemText": "Merge Two Sorted Lists (NeetCode). A dummy-head + tail pointer scaffold is provided. Fill the compare-and-append branch: attach whichever of list1/list2 holds the smaller value to tail, advance that list and tail, then splice the leftover tail once one list empties. Watch how the dummy node means the first append needs no special case.",
          "teaches": "The DUMMY-HEAD sentinel pattern, building an output list by appending to a moving tail, and splicing a remaining suffix in O(1) instead of copying it.",
          "engineLevelId": "linked-list/the-zipper",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "linked-list.l7",
          "order": 7,
          "name": "Carry the One",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Add Two Numbers (NeetCode). Write cold: two numbers are stored as digit lists in reverse order. Walk both lists in lockstep, sum digit + digit + carry at each step, build the result behind a dummy head, and remember the final carry can spawn one extra node.",
          "teaches": "Reusing the dummy-head build; walking two lists together while handling unequal lengths; propagating a carry \u2014 the arithmetic cousin of the merge walk.",
          "engineLevelId": "linked-list/carry-the-one",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "linked-list.l8",
          "order": 8,
          "name": "Tortoise & Hare",
          "kind": "guided",
          "difficulty": 3,
          "problemText": "Linked List Cycle (NeetCode). Fill the fast/slow advance and the meeting test: slow hops 1, fast hops 2; if fast or fast.next reaches None there is no loop (return False); if fast == slow the hare has lapped the tortoise (return True). The tracer draws the hare catching the tortoise inside the loop. Note the sidebar: the same 2:1 gait, stopped when fast reaches the end, leaves slow sitting on the MIDDLE node.",
          "teaches": "Floyd's fast/slow (tortoise & hare) for cycle detection AND midpoint-finding, plus the fast.next None-guard that prevents crashing off the end.",
          "engineLevelId": "linked-list/tortoise-hare",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "linked-list.l9",
          "order": 9,
          "name": "Cut the Nth-from-Last",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Remove Nth Node From End of List (NeetCode). Write cold in a single pass: open a gap of n by advancing a fast pointer n steps first, then move fast and slow together until fast falls off the end \u2014 slow now sits just before the target \u2014 and unlink it. Put a dummy head in front so deleting the head node isn't a special case.",
          "teaches": "The fixed-gap two-pointer trick to reach 'k from the end' in one pass, and using the dummy head to erase the delete-the-head edge case.",
          "engineLevelId": "linked-list/cut-the-nth-from-last",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "linked-list.l10",
          "order": 10,
          "name": "Fold the List",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Reorder List (NeetCode). Write cold by composing three skills you already own: use fast/slow to find the middle, reverse the second half, then interleave the two halves node by node (1st, last, 2nd, 2nd-last, ...). Must be in place, O(1) extra space.",
          "teaches": "Decomposing a hard problem into practiced sub-routines (find-middle + reverse + zipper-merge); managing several live pointers at once without copying nodes.",
          "engineLevelId": "linked-list/fold-the-list",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "linked-list.l11",
          "order": 11,
          "name": "The K-Way Weld",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Merge K Sorted Lists (NeetCode). Beat the brown-out. The obvious approach \u2014 merge list 2 into 1, then 3 into that, then 4... \u2014 re-walks the ever-growing accumulator and trips the op-meter at O(k*n). Refactor to pairwise rounds: merge lists in disjoint pairs, halving the count each round, to land at O(n log k) and clear the power meter.",
          "teaches": "Divide-and-conquer / pairwise reduction, why sequential accumulation degrades to quadratic, and reusing your Merge-Two-Sorted-Lists as the inner subroutine.",
          "engineLevelId": "linked-list/the-k-way-weld",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "linked-list.l12",
          "order": 12,
          "name": "BOSS: The Cache Vault",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "LRU Cache (NeetCode). Design a fixed-capacity cache with O(1) get and put. Fuse this chapter's list mechanism with the hash map from Arrays & Hashing: a hashmap maps key -> node for O(1) lookup, and a DOUBLY-linked list with dummy head+tail sentinels holds usage order so the least-recently-used node is always at the tail and evicting it is O(1). On get/put, unlink the touched node and re-insert it at the head.",
          "teaches": "The doubly-linked list with sentinel nodes (O(1) unlink + insert-at-head), and marrying a linked list to a hashmap to hit O(1) on both operations \u2014 the canonical data-structure-combination interview problem.",
          "engineLevelId": "linked-list/boss-the-cache-vault",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "recursion",
      "actId": "linked-recursive",
      "order": 2,
      "title": "Recursion & the Call Stack",
      "oneLineGoal": "Read, write, and reason about recursive solutions by seeing the call stack as a physical tower of frames \u2014 base case, the recursive leap of faith, and return values unwinding back up.",
      "mechanism": "A function that calls itself builds a tower of stack frames \u2014 each frame solves a smaller subproblem, the descent stops at a base case, then the tower unwinds and carries return values back up.",
      "prereqChapters": [
        "foundations",
        "stack",
        "linked-list",
        "two-pointers"
      ],
      "bossCombinesWith": [
        "two-pointers"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "recursion.l1",
          "order": 1,
          "name": "The Tower of Frames",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "Poke to call factorial(4). Each call drops a new frame onto the call-stack tower (a crate holding that call's n); at n<=1 the base case lights up; then frames pop one by one, each multiplying the value handed up from the frame below. No code to write \u2014 scrub back and forth and watch the tower grow, stop, and unwind.",
          "teaches": "What a stack frame is, that a function can call itself, the base case that halts the descent, and LIFO unwinding \u2014 the call stack IS the Stack substrate you already know, now built by the language itself.",
          "engineLevelId": "recursion/the-tower-of-frames",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "recursion.l2",
          "order": 2,
          "name": "Down, Then Back Up",
          "kind": "tutorial",
          "difficulty": 2,
          "problemText": "Watch recursive sum_to(n) that adds 1..n. Frames stack downward doing no arithmetic; the base case returns 0; then each frame adds its own number to the running total bubbling up. Toggle a variant that places the work BEFORE vs AFTER the recursive call and watch the print order flip.",
          "teaches": "The 'leap of faith' \u2014 trust the recursive call to solve the smaller case \u2014 and WHERE work happens: on the way down (before the call) vs on the way up (after the call, using the returned value).",
          "engineLevelId": "recursion/down-then-back-up",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "recursion.l3",
          "order": 3,
          "name": "The Branching Stack",
          "kind": "tutorial",
          "difficulty": 2,
          "problemText": "Watch naive fib(6): each frame spawns TWO child calls, so the stack becomes a call TREE, not a straight line. The tracer highlights how fib(3), fib(2)... get recomputed over and over; the op/step meter spikes and the power meter starts dipping toward brown-out.",
          "teaches": "A recursive case can make multiple calls (branching recursion / call tree), and overlapping subproblems make naive recursion explode to O(2^n) \u2014 the exact problem you'll repair in the Optimize level.",
          "engineLevelId": "recursion/the-branching-stack",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "recursion.l4",
          "order": 4,
          "name": "Mind the Base Case",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "A recursive reverse-a-string function is missing its base case. Run it first and watch the tower climb without end until the power browns out (RecursionError / stack overflow). Then fill the one missing line \u2014 the base case (len(s) <= 1 -> return s) that stops the descent.",
          "teaches": "Every recursion needs a base case that both shrinks toward AND actually reaches a stopping condition; omit it and the tower overflows and collapses.",
          "engineLevelId": "recursion/mind-the-base-case",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "recursion.l5",
          "order": 5,
          "name": "Recurse Down the Chain",
          "kind": "guided",
          "difficulty": 3,
          "problemText": "Recursive length of a linked list. The base case (empty node -> 0) is given; fill the single recursive line that returns 1 + length(node.next), and watch a frame get pushed for every node as the robot walks the chain, then a count add up on the unwind.",
          "teaches": "Applying recursion to the linked-list substrate: each frame owns exactly one node and defers 'the rest of the list' to the recursive call \u2014 linear recursion over a pointer chain.",
          "engineLevelId": "recursion/recurse-down-the-chain",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "recursion.l6",
          "order": 6,
          "name": "Merge Two Chains",
          "kind": "open",
          "difficulty": 3,
          "problemText": "NeetCode 'Merge Two Sorted Lists' \u2014 write it recursively, cold. Base case: if either list is empty, return the other. Otherwise pick the smaller head, set its .next to the recursively-merged rest, and return it.",
          "teaches": "Designing a recursive solution from scratch: choose the base case(s), do one tiny piece of work (pick the smaller head), and splice in the result of a recursive call on the remainder.",
          "engineLevelId": "recursion/merge-two-chains",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "recursion.l7",
          "order": 7,
          "name": "Reverse the Chain, Bottom-Up",
          "kind": "open",
          "difficulty": 4,
          "problemText": "NeetCode 'Reverse Linked List' \u2014 write the recursive version cold. Recurse all the way to the tail (the new head), then on the way UP rewire each node: node.next.next = node; node.next = None.",
          "teaches": "Doing the real work on the way UP (post-order) and threading pointers as frames unwind \u2014 the trickier half of recursion, where a returned result and pointer side-effects combine in one pass.",
          "engineLevelId": "recursion/reverse-the-chain-bottom-up",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "recursion.l8",
          "order": 8,
          "name": "Halve and Conquer",
          "kind": "open",
          "difficulty": 4,
          "problemText": "NeetCode 'Pow(x, n)' \u2014 compute x^n cold in O(log n) by halving: half = pow(x, n // 2); return half * half (times x more if n is odd), with base case n == 0 -> 1 and negative n handled by inverting.",
          "teaches": "Divide-and-conquer recursion: each call halves the problem so the tower is only ~log(n) frames tall \u2014 the depth idea that powers merge sort and binary search.",
          "engineLevelId": "recursion/halve-and-conquer",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "recursion.l9",
          "order": 9,
          "name": "Cache the Tree",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Return to the naive Fibonacci from 'The Branching Stack' \u2014 it browns out around fib(35). Beat the power meter by memoizing: store each fib(k) in a dict the first time it's computed and reuse it, collapsing the branching call tree from O(2^n) into a thin O(n) line.",
          "teaches": "Killing overlapping subproblems with memoization \u2014 prune the exploding call tree down to a single lineage of frames. This is the seed that grows into Dynamic Programming.",
          "engineLevelId": "recursion/cache-the-tree",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "recursion.l10",
          "order": 10,
          "name": "Merge Sort: Split, Solve, Merge",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "NeetCode 'Sort an Array' via merge sort, cold. Recursively split the array in half (divide-and-conquer, the same halving as Pow) down to single elements, then merge each pair of sorted halves back together with a two-pointer merge on the way up.",
          "teaches": "Fusing the whole chapter: divide-and-conquer recursion (O(log n) depth) plus the two-pointer merge to sort in O(n log n) \u2014 a complete recursive algorithm assembled from the chapter's parts and an earlier pattern.",
          "engineLevelId": "recursion/merge-sort-split-solve-merge",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "trees",
      "actId": "linked-recursive",
      "order": 3,
      "title": "Trees & BSTs \u2014 The Recursive Grove",
      "oneLineGoal": "Solve any binary-tree problem with one recursive template (dive \u2192 combine \u2192 return, or pass constraints down), reach for a queue when you must sweep level by level, and exploit BST ordering to skip half the work.",
      "mechanism": "A tree is just linked-list nodes with two pointers, and almost every tree problem is one recursive function that trusts itself on the left and right children then combines their answers on the way back up (post-order) or pushes constraints down on the way in (pre-order) \u2014 with a queue substrate for level-by-level sweeps.",
      "prereqChapters": [
        "foundations",
        "recursion",
        "linked-list"
      ],
      "bossCombinesWith": [
        "arrays-hashing"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "trees.l1",
          "order": 1,
          "name": "The Grove",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "Guided tour of a prebuilt binary tree: every node is a numbered crate and .left/.right are two robot arms. Poke nodes to light up their children; drop a walker robot at the root and watch it descend to a leaf. Tap crates to label root, leaves, depth, and height. No code, no fail.",
          "teaches": "Tree anatomy (root / parent / child / leaf, depth vs height) and the key reframe: a TreeNode is just a linked-list node with two 'next' arms instead of one \u2014 not a brand-new substrate.",
          "engineLevelId": "trees/the-grove",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "trees.l2",
          "order": 2,
          "name": "Deep Dive",
          "kind": "tutorial",
          "difficulty": 1,
          "problemText": "Maximum Depth of Binary Tree (LC 104). Watch the tracer run maxDepth(root): the call stack grows as the walker dives to the deepest leaf, None returns 0, and each node returns 1 + max(left, right) as the stack unwinds. Scrub to replay the descent and the combine.",
          "teaches": "The recursive tree template and the call-stack mental model \u2014 base case at None, recurse into both children, combine the two answers on the way back up (post-order).",
          "engineLevelId": "trees/deep-dive",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "trees.l3",
          "order": 3,
          "name": "Mirror, Mirror",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Invert Binary Tree (LC 226). Fill the one missing line that swaps node.left and node.right; the surrounding recursion is given and will flip the entire tree once your line is right.",
          "teaches": "Do-something-at-the-node then recurse into the children; recursion can mutate a structure, not only read it.",
          "engineLevelId": "trees/mirror-mirror",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "trees.l4",
          "order": 4,
          "name": "Spot the Difference",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Same Tree (LC 100). Fill the recursive return that ANDs the root-value check with isSame(p.left, q.left) and isSame(p.right, q.right); the None/None and None/node base cases are provided.",
          "teaches": "Dual/parallel recursion walking two trees in lockstep, and handling the mismatched-shape base cases with None.",
          "engineLevelId": "trees/spot-the-difference",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "trees.l5",
          "order": 5,
          "name": "Grove Within a Grove",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Subtree of Another Tree (LC 572). Write it cold: at every node of the big tree, call your Same-Tree helper to test whether the subtree rooted there equals t.",
          "teaches": "Nesting one recursion (isSameTree) inside a traversal and reusing an already-solved sub-problem \u2014 a direct callback to level 4.",
          "engineLevelId": "trees/grove-within-a-grove",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "trees.l6",
          "order": 6,
          "name": "The Long Way Round",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Diameter of Binary Tree (LC 543). Write it cold: a height() recursion that returns each node's height upward while updating a nonlocal best = max(best, left_height + right_height) at every node.",
          "teaches": "The workhorse pattern \u2014 return one value up the stack while tracking a separate global answer as the recursion unwinds.",
          "engineLevelId": "trees/the-long-way-round",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "trees.l7",
          "order": 7,
          "name": "Tipping Point",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Balanced Binary Tree (LC 110). Write it cold in a single pass: height() returns the true height, or -1 the moment any node's two child heights differ by more than one, short-circuiting the rest of the tree.",
          "teaches": "Bubbling a failure signal (-1 sentinel) up through post-order to get an O(n) one-pass answer instead of the naive O(n^2) recompute-height-at-every-node.",
          "engineLevelId": "trees/tipping-point",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "trees.l8",
          "order": 8,
          "name": "The Loading Dock",
          "kind": "tutorial",
          "difficulty": 2,
          "problemText": "Binary Tree Level Order Traversal (LC 102). Watch BFS run: a queue is a conveyor belt of robots. Snapshot len(queue) at the start of each level to learn how many crates belong to that row, drain exactly those, and enqueue their children behind them.",
          "teaches": "The chapter's second core tool \u2014 a queue for breadth-first, level-by-level sweeps \u2014 and the len(queue)-snapshot trick that cuts the stream into distinct rows.",
          "engineLevelId": "trees/the-loading-dock",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "trees.l9",
          "order": 9,
          "name": "The View From the Right",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Binary Tree Right Side View (LC 199). Write it cold with BFS: for each level, keep only the last node dequeued from that row.",
          "teaches": "Applying per-level BFS logic (caring about a node's position within its level) \u2014 hands-on practice with the queue tool.",
          "engineLevelId": "trees/the-view-from-the-right",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "trees.l10",
          "order": 10,
          "name": "Kings of the Hill",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Count Good Nodes in Binary Tree (LC 1448). Write it cold: a DFS that carries max-so-far DOWN as a parameter and counts every node whose value is >= that running max.",
          "teaches": "Passing state DOWN into the recursion (pre-order accumulation) \u2014 the opposite direction from levels 6\u20137's return-up \u2014 which sets up passing constraints down for BSTs.",
          "engineLevelId": "trees/kings-of-the-hill",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "trees.l11",
          "order": 11,
          "name": "Fork in the Road",
          "kind": "guided",
          "difficulty": 3,
          "problemText": "Lowest Common Ancestor of a BST (LC 235). Fill the branch logic: if both target values are less than the current node go left, if both are greater go right, otherwise this node is the split point and is the LCA.",
          "teaches": "The BST ordering invariant \u2014 because left < node < right, one comparison chooses a single direction and discards half the tree, giving an O(h) walk with no full search.",
          "engineLevelId": "trees/fork-in-the-road",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "trees.l12",
          "order": 12,
          "name": "Border Patrol",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Validate Binary Search Tree (LC 98). Write it cold: recurse passing a valid (low, high) range down; every node must fit strictly inside it, going left tightens high to the node's value and going right tightens low. (The naive local left < root < right check is the trap this level exposes.)",
          "teaches": "Carrying constraints DOWN the recursion and why a propagated global range \u2014 not a local parent-only check \u2014 is what actually enforces the BST property.",
          "engineLevelId": "trees/border-patrol",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "trees.l13",
          "order": 13,
          "name": "Just Enough",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Kth Smallest Element in a BST (LC 230). Beat the brown-out: the naive solve dumps a full in-order traversal into a list and indexes k-1. Hit the op/step budget instead with an in-order walk that stops the instant it has counted k nodes.",
          "teaches": "In-order traversal of a BST yields values in sorted order, and early-exit turns O(n) into O(h + k) work \u2014 enough to keep the power meter out of the red.",
          "engineLevelId": "trees/just-enough",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "trees.l14",
          "order": 14,
          "name": "Rebuild the Grove",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "Construct Binary Tree from Preorder and Inorder Traversal (LC 105). Reconstruct the tree: preorder[0] is the root, find it in inorder to split the left and right subtrees, then recurse on the matching preorder/inorder slices. Fuse in a hashmap of value \u2192 inorder-index (from arrays-hashing) so each split is O(1), turning the O(n^2) rescan into O(n) and clearing the brown-out.",
          "teaches": "Rebuilding structure from traversal orders (preorder gives roots, inorder gives the left/right split) while fusing a hash map to eliminate the repeated linear scan \u2014 recursion mastery plus an earlier pattern.",
          "engineLevelId": "trees/rebuild-the-grove",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "heaps",
      "actId": "linked-recursive",
      "order": 4,
      "title": "Heaps & Priority Queues: The Loosely-Sorted Pile",
      "oneLineGoal": "Master the binary heap / priority queue so the extreme element is always instantly reachable, and use the size-k and two-heap tricks to solve top-k, streaming, and scheduling problems without ever fully sorting the data.",
      "mechanism": "A heap keeps a pile only loosely sorted \u2014 just enough that the smallest (or largest) crate always sits on top \u2014 so grabbing the extreme costs O(1) and re-settling the pile after a push or pop costs O(log n) by bubbling a single crate up or down one root-to-leaf path.",
      "prereqChapters": [
        "foundations",
        "arrays-hashing",
        "trees"
      ],
      "bossCombinesWith": [
        "arrays-hashing"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "heaps.l1",
          "order": 1,
          "name": "THE BUBBLE PIT",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "No-fail sandbox: crates sit on a backing tape (the array) with a pyramid drawn over it (index 0 at the apex, 1 & 2 below, 3-6 below that). Poke 'add' to drop a crate at the end of the tape and watch it bubble UP past any bigger parent; poke 'take top' to lift the apex crate off, move the last tape crate onto the apex, and watch it sink DOWN past its smaller child; poke 'peek' to see the minimum always waiting at index 0; poke 'heapify' to drop a scrambled tape and watch it settle into a valid pile. Nothing to solve \u2014 just poke and watch the meters tick.",
          "teaches": "A heap is a complete binary tree packed into an array (parent i -> children 2i+1, 2i+2; child i -> parent (i-1)//2); the min-heap invariant (every parent <= its children) is exactly why the smallest crate is always at index 0; sift-up on push and sift-down on pop each touch only ONE root-to-leaf path, so both are O(log n); 'loosely sorted' means siblings are unordered \u2014 a heap is not a sorted array.",
          "engineLevelId": "heaps/the-bubble-pit",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "heaps.l2",
          "order": 2,
          "name": "STONE SMASHER",
          "kind": "tutorial",
          "difficulty": 2,
          "problemText": "Last Stone Weight (NeetCode Easy): repeatedly smash the two heaviest stones; if they differ, the difference goes back into the pile; return the last stone's weight (or 0). Scrub through a prewritten solution frame-by-frame: a max-heap pops the top two crates, pushes back their difference, and repeats until one or zero stones remain.",
          "teaches": "Python's heapq is a MIN-heap, so you store NEGATED weights to simulate a MAX-heap and negate back on the way out; the extract-max loop (heappop twice, heappush the remainder); reading the op/step meter to see each heappush/heappop cost O(log n) while the whole run is O(n log n).",
          "engineLevelId": "heaps/stone-smasher",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "heaps.l3",
          "order": 3,
          "name": "THE HIGH-WATER MARK",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Kth Largest Element in a Stream (NeetCode Easy): the KthLargest class returns the kth largest value seen so far after each add(). The scaffold already heapifies the initial list; fill the ONE missing line in add() that pushes the new value and then evicts the smallest whenever the heap grows past size k, so the kth-largest stays parked at the root.",
          "teaches": "A size-k MIN-heap keeps the kth largest element sitting at its root (index 0) \u2014 everything smaller has already fallen out; heapify(list) builds the starting pile in O(n); heappushpop (or push-then-pop-if-len>k) caps the heap so each add() is O(log k), not O(n).",
          "engineLevelId": "heaps/the-high-water-mark",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "heaps.l4",
          "order": 4,
          "name": "TOP OF THE PILE",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Kth Largest Element in an Array (NeetCode Medium): return the kth largest element of an unsorted array. Write it cold with a heap \u2014 either sweep the array through a size-k min-heap, or heapify the whole array as a max-heap and pop k-1 times.",
          "teaches": "The top-k pattern: a size-k heap answers 'kth largest' in O(n log k), beating a full O(n log n) sort when k is small (the brown-out spine rewards it); choosing min-heap-of-size-k versus max-heap-of-all is a deliberate time/space trade-off.",
          "engineLevelId": "heaps/top-of-the-pile",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "heaps.l5",
          "order": 5,
          "name": "NEAREST BEACONS",
          "kind": "open",
          "difficulty": 3,
          "problemText": "K Closest Points to Origin (NeetCode Medium): return the k points nearest the origin by Euclidean distance. Write it cold using a heap of (distance, point) crates.",
          "teaches": "Heaps order by a computed KEY by storing (key, payload) tuples, which compare lexicographically; keeping the k SMALLEST items means a size-k MAX-heap (store negated distances) that evicts the farthest crate as new points arrive; use squared distance to skip the sqrt. Same top-k skeleton as the previous level, now keyed.",
          "engineLevelId": "heaps/nearest-beacons",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "heaps.l6",
          "order": 6,
          "name": "THE COOLDOWN YARD",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Task Scheduler (NeetCode Medium): given task labels and a cooldown n between two identical tasks, return the minimum time units to finish everything. Write it cold: count tasks with a hashmap, load the counts into a max-heap, then each tick run the most-frequent AVAILABLE task and park the just-run task in a FIFO cooldown queue until its timer expires.",
          "teaches": "Combining a max-heap (always grab the most-frequent ready task) with a cooldown QUEUE that re-admits a task once its timer elapses \u2014 two structures interlocking; feeding a hashmap of counts (from arrays-hashing) into a heap; simulating time ticks where forced idles are the visible brown-out cost of the greedy choice.",
          "engineLevelId": "heaps/the-cooldown-yard",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "heaps.l7",
          "order": 7,
          "name": "THE MEDIAN GATE",
          "kind": "optimize",
          "difficulty": 5,
          "problemText": "Find Median from Data Stream (NeetCode Hard): support addNum() and findMedian() over a growing stream. The naive 'store everything and sort on every findMedian' solution trips the brown-out meter at scale; hit the target by holding a MAX-heap for the lower half and a MIN-heap for the upper half, kept balanced so the median is always at (or between) the two roots.",
          "teaches": "Two balanced heaps split the stream at the median: findMedian becomes O(1) (peek one or both roots) and addNum stays O(log n), versus O(n log n) per query for re-sorting; the whole trick is maintaining the size invariant (|low| - |high| <= 1) by shuffling one crate between the piles after each insert.",
          "engineLevelId": "heaps/the-median-gate",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "heaps.l8",
          "order": 8,
          "name": "THE FEED FORGE",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "Design Twitter (NeetCode Medium): implement postTweet, follow, unfollow, and getNewsFeed that returns the 10 most recent tweets across a user and everyone they follow. Fuse the heap merge with the hashing from Arrays & Hashing: hashmaps store user->tweets and user->followees, a global counter timestamps tweets, and a heap merges the k time-ordered tweet lists to pull only the newest 10.",
          "teaches": "Merging k already-ordered tweet lists with a heap (the merge-k-lists technique) instead of sorting every feed; a monotonic global timestamp gives a total order across users; hashmaps (arrays-hashing) hold the follow graph and per-user tweet history \u2014 the boss where the size-k / merge heap pattern rides on top of the hashing substrate you already own.",
          "engineLevelId": "heaps/the-feed-forge",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "backtracking",
      "actId": "search-optimize",
      "order": 1,
      "title": "Backtracking: Choose, Explore, Un-choose",
      "oneLineGoal": "Turn any \"find all combinations / arrangements that satisfy a constraint\" problem into a choose-explore-unchoose search over a decision tree, then prune it so it runs fast.",
      "mechanism": "Every backtracking problem is a depth-first walk over an implicit tree of choices: at each node you make one choice, recurse to explore everything that follows from it, then un-make it to try the next \u2014 pruning any branch that can't reach a valid answer.",
      "prereqChapters": [
        "foundations",
        "recursion",
        "trees",
        "arrays-hashing"
      ],
      "bossCombinesWith": [
        "arrays-hashing"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "backtracking.l1",
          "order": 1,
          "name": "Choose, Explore, Un-choose",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "No code. Given three numbered crates [1,2,3], the learner pokes an INCLUDE or SKIP switch on each crate in turn. A robot walks down a decision tree that draws itself branch by branch; at each leaf it drops the current path into a results bin. The learner can rewind and try the other switches, watching the path-stack substrate grow on the way down and shrink (pop) as the robot walks back up. Goal: light up all 8 leaves.",
          "teaches": "The core backtracking loop \u2014 make a choice, recurse to explore its consequences, then undo it (backtrack) to try the next; the recursion tree's leaves are the complete answers, and the path stack is what grows on descent and shrinks on the way back up.",
          "engineLevelId": "backtracking/choose-explore-un-choose",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "backtracking.l2",
          "order": 2,
          "name": "Subsets, on Autopilot",
          "kind": "tutorial",
          "difficulty": 1,
          "problemText": "Subsets (LC 78): watch the standard backtracking solution run on nums=[1,2,3] with the scrubber. The code panel highlights the active line while the recursion tree, the path stack, and the results list animate in sync. Emphasis on the start index i that keeps the robot moving forward along the tape, and the res.append(path[:]) that snapshots a COPY at every node.",
          "teaches": "The canonical backtracking template (path + start index + collect at each node) and why we append a copy of the path rather than the live, still-mutating list.",
          "engineLevelId": "backtracking/subsets-on-autopilot",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "backtracking.l3",
          "order": 3,
          "name": "The Missing Undo",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Subsets (LC 78): the solution is complete except for one blank line at the end of the loop body. Run it once as-is and the animation shows the path stack growing forever and duplicated garbage piling into results. Fill in the single line \u2014 path.pop() \u2014 and watch the stack correctly shrink each time recursion returns.",
          "teaches": "The un-choose step is what keeps the shared path in sync as recursion unwinds; skipping the pop corrupts every sibling branch that runs after it.",
          "engineLevelId": "backtracking/the-missing-undo",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "backtracking.l4",
          "order": 4,
          "name": "Dialing by Branches",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Letter Combinations of a Phone Number (LC 17): digits like \"23\" map to letter groups (2->abc, 3->def). The template is provided; fill in the loop that iterates over mapping[digits[i]] and recurses to the next digit. The tree drawn on screen now branches k-ways instead of two, one level per digit, with fixed depth = len(digits).",
          "teaches": "Branching factor can be more than two and can come from a lookup map; when depth is fixed and there is no constraint to violate, backtracking is simply a clean Cartesian product over the choice tree.",
          "engineLevelId": "backtracking/dialing-by-branches",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "backtracking.l5",
          "order": 5,
          "name": "Order Matters Now",
          "kind": "guided",
          "difficulty": 3,
          "problemText": "Permutations (LC 46): unlike subsets, every element must appear and order counts, so there is no start index \u2014 each level chooses from ALL elements not yet used. A `used` boolean track (a row of robot flags over the crates) is provided; fill in the guard that skips an element when used[i] is True, plus the matching set-true-before / reset-false-after around the recursive call.",
          "teaches": "When order matters you choose from all remaining elements instead of a forward suffix, using a `used` marker to avoid repeats \u2014 the direct contrast to the start-index pattern from subsets.",
          "engineLevelId": "backtracking/order-matters-now",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "backtracking.l6",
          "order": 6,
          "name": "Hitting a Target",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Combination Sum (LC 39): given distinct candidates and a target, write the solver cold. Numbers may be reused, so the recursive call passes the SAME start index i (not i+1). Two base cases: when the running remainder hits 0, collect a copy; when it goes negative, abandon the branch. The op-meter counts every node the robot visits.",
          "teaches": "Element reuse (recurse on i, not i+1) and the first real pruning: a base case that abandons a branch the instant the running total overshoots the target.",
          "engineLevelId": "backtracking/hitting-a-target",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "backtracking.l7",
          "order": 7,
          "name": "Killing the Duplicates",
          "kind": "guided",
          "difficulty": 3,
          "problemText": "Combination Sum II (LC 40): now candidates contain duplicates and each may be used at most once (recurse with i+1). The solution sorts the candidates first; fill in the one guard that skips a candidate equal to its left neighbor at the SAME tree level \u2014 `if i > start and candidates[i] == candidates[i-1]: continue`. The tree view flags the duplicate sibling branches that get pruned.",
          "teaches": "The sort-then-skip-equal-sibling trick for deduping results when the input has repeats: skip a value only when it repeats at the same level, never when it repeats deeper down a branch.",
          "engineLevelId": "backtracking/killing-the-duplicates",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "backtracking.l8",
          "order": 8,
          "name": "Cutting a String",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Palindrome Partitioning (LC 131): write it cold. At index start, the choices are every possible cut end; a cut is allowed only if the substring s[start:end] is a palindrome (a provided is_palindrome helper). Recurse from end; collect a copy when start reaches the end of the string. The tape shows the string being sliced, with non-palindrome cuts greyed out before the robot descends.",
          "teaches": "Choices can be variable-length substrings gated by a validity test; backtracking enumerates every valid partition by descending only into branches that pass the check.",
          "engineLevelId": "backtracking/cutting-a-string",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "backtracking.l9",
          "order": 9,
          "name": "Prune or Brown Out",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Combination Sum (LC 39), stress input: your level-6 solution now trips the brown-out power meter because it keeps wandering into branches whose remainder is already negative for many more candidates. Sort the candidates ascending and add `if candidates[i] > remaining: break` inside the loop so the entire rest of the branch is cut at once. Beat the op-budget shown on the meter.",
          "teaches": "Pruning is the main performance lever in backtracking: sorting lets you break (kill all larger siblings in one shot) instead of continue, collapsing the explored tree from thousands of nodes to a fraction.",
          "engineLevelId": "backtracking/prune-or-brown-out",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "backtracking.l10",
          "order": 10,
          "name": "Search the Grid",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Word Search (LC 79): the substrate becomes a 2D grid of lettered crates. Write the DFS-backtracking cold: from a matching cell, mark it visited (temporarily overwrite with '#'), explore the four neighbors for the next character, and on return un-mark it back to its letter. Out-of-bounds and letter-mismatch checks prune immediately. A robot leaves a glowing trail that lights on the way in and goes dark on backtrack.",
          "teaches": "Backtracking generalizes from a 1D tape to a 2D grid: the 'choices' are the four neighbors, and the un-choose step is un-marking the visited cell so other paths can reuse it.",
          "engineLevelId": "backtracking/search-the-grid",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "backtracking.l11",
          "order": 11,
          "name": "N-Queens",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "N-Queens (LC 51): place n queens on an n x n board so none attack each other. Backtrack row by row; at each row try every column, but use three hash sets \u2014 columns, the (row - col) diagonal, and the (row + col) anti-diagonal \u2014 for O(1) conflict checks and instant pruning. This fuses the choose-explore-unchoose loop with the set-membership pattern from Arrays & Hashing. Beat the brown-out on n = 8 and above.",
          "teaches": "Constraint-satisfaction backtracking: combine the decision-tree search with O(1) hash-set membership (columns plus both diagonals) to prune illegal placements immediately \u2014 the capstone fusion of backtracking and hashing.",
          "engineLevelId": "backtracking/n-queens",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "graphs",
      "actId": "search-optimize",
      "order": 2,
      "title": "Graphs \u2014 BFS & DFS: Floods, Dives, and Frontiers",
      "oneLineGoal": "Traverse graphs and grids with BFS and DFS to count and measure regions, find shortest paths, detect cycles, and order dependencies.",
      "mechanism": "To explore a graph you visit a start node then fan out to its neighbors, stamping each node visited so you never loop \u2014 BFS uses a queue to spread in equal-distance rings (shortest paths) while DFS uses recursion or a stack to plunge down one path and backtrack.",
      "prereqChapters": [
        "foundations",
        "arrays-hashing",
        "recursion",
        "trees",
        "stack"
      ],
      "bossCombinesWith": [
        "arrays-hashing"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "graphs.l1",
          "order": 1,
          "name": "Signal in the Network",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "Guided-tour sandbox, nothing to code and nothing to fail. The screen shows a network of numbered junction-box nodes wired together, and beside it a grid of crates. Poke any node: its wires light up and its direct neighbors glow \u2014 that is adjacency. Drag the Wavefront robot onto a node and tap 'ripple' \u2014 it floods outward one ring at a time (BFS). Drag the Diver robot on and tap 'dive' \u2014 it plunges down one path leaving a paint stamp, hits a dead end, and backtracks (DFS). Toggle the grid view to see that a grid cell is just a node whose neighbors are up/down/left/right.",
          "teaches": "Graph vocabulary from zero \u2014 nodes, edges, neighbors, adjacency; that a grid IS a graph (each cell a node with 4 neighbors); the visited stamp that stops infinite loops; and the core split between BFS (spread in rings via a queue) and DFS (plunge and backtrack via a stack/recursion).",
          "engineLevelId": "graphs/signal-in-the-network",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "graphs.l2",
          "order": 2,
          "name": "Two Robots, One Grid",
          "kind": "tutorial",
          "difficulty": 1,
          "problemText": "Watch mode on a small Number of Islands grid (1 = land crate, 0 = water). Press play: the Diver DFS sinks one island \u2014 it stamps a land cell visited, then recurses into each of its 4 neighbors, turning the whole connected blob dark, and the op-meter ticks once per cell. Replay the same grid with the Wavefront BFS to see the identical blob filled ring-by-ring from a queue. The code panel highlights the active line as the robot moves.",
          "teaches": "Flood-filling one connected region; marking cells visited so each is touched exactly once; that DFS and BFS discover the SAME set of reachable cells and only the order differs; reading the op-meter and the active-line highlight.",
          "engineLevelId": "graphs/two-robots-one-grid",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "graphs.l3",
          "order": 3,
          "name": "Sink the Island",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Number of Islands, full grid. The scaffold is written: the outer double loop, the count increment, and a dfs(r, c) whose base case (out of bounds, or water/already-visited) is given \u2014 but the recursive step is blank. Fill the ONE missing line that stamps the current cell and dives into its four neighbors so the island fully sinks before count += 1.",
          "teaches": "Writing the recursive neighbor expansion for grid DFS; why you stamp a cell BEFORE recursing (so you never re-enter it); that each fresh unvisited land cell is the seed of a new component to count.",
          "engineLevelId": "graphs/sink-the-island",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "graphs.l4",
          "order": 4,
          "name": "How Big Is It?",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Max Area of Island, blank editor. Write a DFS that returns the SIZE of the island containing a cell (1 plus the sum of the four neighbor dives), scan every cell, and report the largest area found. No scaffold.",
          "teaches": "Writing grid DFS from scratch; a DFS that RETURNS an aggregate value (area) assembled from its child calls rather than just marking cells; tracking a running maximum across separate components.",
          "engineLevelId": "graphs/how-big-is-it",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "graphs.l5",
          "order": 5,
          "name": "Trapped in the Middle",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Surrounded Regions: flip every 'O' region fully enclosed by 'X' to 'X', but leave any 'O' that touches the border alone. Write DFS that starts ONLY from the border O's to paint them 'safe', then sweep the board flipping unpainted O's to X and restoring the safe ones.",
          "teaches": "Seeding the search from the border instead of the interior; the invert trick (mark what to KEEP, then flip everything else); launching many DFS starts from a set of seed cells rather than one.",
          "engineLevelId": "graphs/trapped-in-the-middle",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "graphs.l6",
          "order": 6,
          "name": "Two Oceans",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Pacific Atlantic Water Flow: find every cell from which water can reach BOTH oceans, where water flows only to equal-or-lower neighbors. Reverse the flow \u2014 run one traversal inward from the Pacific border cells and another from the Atlantic border cells (climbing to equal-or-higher neighbors), then return the cells reachable in both sets.",
          "teaches": "Reversing the search direction to avoid a fresh search from every cell; running two independent multi-seed traversals and intersecting their reachable sets; encoding a flow rule as the neighbor-eligibility condition.",
          "engineLevelId": "graphs/two-oceans",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "graphs.l7",
          "order": 7,
          "name": "Wire the Stations",
          "kind": "guided",
          "difficulty": 3,
          "problemText": "Number of Connected Components in an Undirected Graph: given n nodes and an edge list. The DFS-count loop is already written; fill the two lines that BUILD the adjacency list from the edge list (add each edge in BOTH directions) so the traversal has neighbors to follow.",
          "teaches": "Representing a non-grid graph as an adjacency list (dict of node -> neighbor list); why undirected edges are added both ways; that counting connected components is the exact same 'start a DFS from each unvisited node' idea as counting islands, just without the grid.",
          "engineLevelId": "graphs/wire-the-stations",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "graphs.l8",
          "order": 8,
          "name": "Copy the Whole Network",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Clone Graph: given a reference to a node in a connected undirected graph, return a deep copy. Write a DFS (or BFS) that carries a hash map old-node -> new-node: on first visit, create the clone and recurse to wire up its neighbor clones; on a revisit, just return the clone that already exists.",
          "teaches": "Traversing an adjacency-based graph via node references; using a hash map as BOTH the visited set and the old->new lookup; how that map breaks cycles by returning the existing clone \u2014 the arrays-hashing pattern fused with traversal.",
          "engineLevelId": "graphs/copy-the-whole-network",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "graphs.l9",
          "order": 9,
          "name": "The Ripple Is the Distance",
          "kind": "tutorial",
          "difficulty": 2,
          "problemText": "Watch mode on a Rotting Oranges grid. Every rotten orange is dropped into the BFS queue at minute 0 all at once; press play and watch the rot spread as a single expanding wavefront where each new ring is minute + 1. The step meter shows every cell entering the queue exactly once, and the brown-out power meter stays green because nothing is re-scanned.",
          "teaches": "BFS layers equal shortest distance in an unweighted graph; MULTI-SOURCE BFS (seed many starts into the queue at once); why BFS, not DFS, measures shortest spread time; visual proof that the visited set keeps the work O(cells). A deliberate difficulty relax to watch a brand-new mechanic before writing it.",
          "engineLevelId": "graphs/the-ripple-is-the-distance",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "graphs.l10",
          "order": 10,
          "name": "Rot Spreads Fastest by Wave",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Rotting Oranges: write the multi-source BFS cold. Seed all rotten cells into the queue and count the fresh ones; BFS minute by minute, rotting fresh neighbors as the wave reaches them; return the minute the last orange rots, or -1 if any fresh orange is unreachable.",
          "teaches": "Writing multi-source BFS from scratch; advancing a level counter once per queue-layer (not per cell); using a fresh-count to detect unreachable cells and correctly return -1.",
          "engineLevelId": "graphs/rot-spreads-fastest-by-wave",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "graphs.l11",
          "order": 11,
          "name": "Fill Every Room",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Walls and Gates: fill each empty room with its distance to the nearest gate. The naive attempt \u2014 a separate BFS/DFS launched from every empty room \u2014 trips the brown-out meter at O((mn)^2). Rewrite it as ONE multi-source BFS seeded from all gates at once and hit the O(mn) target so the power meter stays green.",
          "teaches": "Recognizing 'distance from the nearest of many sources' as a multi-source BFS; flipping the search around (spread FROM the gates, not from each room) to collapse quadratic work to linear; reading the brown-out/op-meter as a live complexity signal.",
          "engineLevelId": "graphs/fill-every-room",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "graphs.l12",
          "order": 12,
          "name": "Can You Finish?",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Course Schedule: given numCourses and prerequisite pairs, decide whether every course can be finished. Build a directed adjacency list and detect whether the graph contains a cycle \u2014 either DFS with a three-state mark (unvisited / on-current-path / done) or Kahn's BFS that repeatedly peels off zero-indegree nodes.",
          "teaches": "DIRECTED graphs (edges point one way) and why any cycle makes finishing impossible; the on-path 'gray' marker that distinguishes a back-edge cycle from a node that is merely already finished; the two standard cycle-detection engines \u2014 DFS 3-color and BFS indegree.",
          "engineLevelId": "graphs/can-you-finish",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "graphs.l13",
          "order": 13,
          "name": "Order of Operations",
          "kind": "optimize",
          "difficulty": 5,
          "problemText": "Course Schedule II: return a valid ordering of all courses, or an empty list if impossible. Extend cycle detection to emit a topological order \u2014 push nodes as they finish in DFS post-order then reverse, or dequeue them in Kahn's order \u2014 while still catching cycles. Hit the O(V + E) target.",
          "teaches": "Topological sort as the PAYLOAD of the same traversal rather than a new algorithm; DFS post-order-reversed versus Kahn's dequeue order; that a valid ordering exists if and only if there is no cycle, all discovered in one linear pass.",
          "engineLevelId": "graphs/order-of-operations",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "graphs.l14",
          "order": 14,
          "name": "The Ladder",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "Word Ladder: transform beginWord into endWord one letter at a time, with every intermediate word in the dictionary, returning the shortest transformation length. Fuse this chapter's BFS-shortest-path engine with the arrays-hashing pattern: pre-index the word list into a hash map of wildcard patterns (e.g. 'h*t' -> [hot, hit]) for O(1) neighbor generation plus a hash set of seen words, then BFS from beginWord counting levels until you reach endWord.",
          "teaches": "Modeling a non-obvious problem AS a graph (words are nodes, single-letter edits are edges); combining BFS-for-shortest-path with hash-based adjacency generation and O(1) membership from arrays-hashing; the full pipeline \u2014 build adjacency, seed the queue, ripple out level by level, and stop the instant you reach the target.",
          "engineLevelId": "graphs/the-ladder",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "tries",
      "actId": "search-optimize",
      "order": 3,
      "title": "Tries \u2014 The Letter Warehouse",
      "oneLineGoal": "Master the prefix tree: build it, walk it, wildcard-search it, and let it supercharge a grid search.",
      "mechanism": "A trie stores a whole set of strings as one branching path of single-letter nodes, so every shared prefix is stored once and any insert, lookup, or prefix-check costs only the length of the word \u2014 never the size of the dictionary.",
      "prereqChapters": [
        "foundations",
        "arrays-hashing",
        "trees",
        "recursion",
        "backtracking"
      ],
      "bossCombinesWith": [
        "backtracking"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "tries.l1",
          "order": 1,
          "name": "The Letter Warehouse",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "Poke-and-watch sandbox built on Implement Trie / Prefix Tree (LC 208): tap to feed the pre-baked words 'cat', 'car', 'card', 'dog' into the warehouse one letter at a time and watch the branching junction-boxes grow; a reader-robot walks from the root down each letter-pipe, and a word-lamp lights on the final letter of each word.",
          "teaches": "The shape of a trie from zero: each node is one letter, each edge is 'the next letter', words that share a prefix share the same path (the 'ca-' trunk splits into 't' and 'r'), and the end-of-word lamp is the only thing that separates a real word 'car' from a mere prefix of 'card'. No code, no failure \u2014 just build intuition for the substrate.",
          "engineLevelId": "tries/the-letter-warehouse",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "tries.l2",
          "order": 2,
          "name": "Insert, Watched",
          "kind": "tutorial",
          "difficulty": 2,
          "problemText": "Step through the real Python insert() of Implement Trie (LC 208) as it stores 'apple' then 'app', with the active line highlighted and the scrubber live.",
          "teaches": "The insert loop as motion: the reader-robot starts at the root and for each character either follows an existing child-pipe or builds a new junction, then descends; at the end it lights the word-lamp. The op-meter ticks once per character, making it visible that insert is O(len(word)) \u2014 flat no matter how many words already live in the warehouse.",
          "engineLevelId": "tries/insert-watched",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "tries.l3",
          "order": 3,
          "name": "Search vs. startsWith",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Fill the one missing line in search() and startsWith() for Implement Trie (LC 208). The walk-the-path scaffold is provided; you supply the 'if char not in node.children: return False' bail-out and the terminal return (return node.is_end for search vs. return True for startsWith).",
          "teaches": "Lookup is just walking the path and failing the instant a letter-pipe is missing; the ONLY difference between search and startsWith is the final check \u2014 search demands the word-lamp be lit, startsWith is happy just to have arrived. Cements why a trie deliberately separates 'is a word' from 'is a prefix'.",
          "engineLevelId": "tries/search-vs-startswith",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "tries.l4",
          "order": 4,
          "name": "Build the Warehouse Cold",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Implement Trie / Prefix Tree (LC 208) from scratch: a TrieNode holding a children dict + an is_end flag, plus insert, search, and startsWith.",
          "teaches": "Assembling the whole structure unaided: node design (a dict of children keyed by single characters), the shared descend-or-create walk reused across all three methods, and the load-bearing mental model that a trie is simply a tree whose edges are labeled by letters. Target complexity O(len(word)) per operation.",
          "engineLevelId": "tries/build-the-warehouse-cold",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "tries.l5",
          "order": 5,
          "name": "Prefix Query Storm",
          "kind": "optimize",
          "difficulty": 3,
          "problemText": "Autocomplete workload built on startsWith (LC 208): a dictionary of ~10^4 words must answer a storm of prefix queries. A naive per-query scan of the word list trips the brown-out power meter (O(N\u00b7L) per query); load the words into a trie once so each query is answered in O(len(prefix)).",
          "teaches": "The headline win of a trie made concrete against the power meter: prefix queries cost the length of the query, not the size of the dictionary. Watching the O(N\u00b7L) list-scan brown out beside the O(len(prefix)) trie-walk builds real Big-O intuition and shows exactly when to reach for a trie instead of a plain hashset.",
          "engineLevelId": "tries/prefix-query-storm",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "tries.l6",
          "order": 6,
          "name": "The Wildcard Split",
          "kind": "tutorial",
          "difficulty": 4,
          "problemText": "Watch the search of Design Add and Search Words Data Structure (LC 211) run on queries with a '.' wildcard \u2014 e.g. searching '.ad' and 'b..' against a trie holding 'bad', 'dad', and 'mad'.",
          "teaches": "How a wildcard turns a straight walk into a recursive branch: at a normal letter the robot follows one pipe, but at a '.' it clones itself into EVERY child-pipe and each clone searches on. Watch the search fan out and prune dead branches \u2014 the bridge from a linear lookup to recursive trie-DFS, reusing the recursion chapter's mental model.",
          "engineLevelId": "tries/the-wildcard-split",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "tries.l7",
          "order": 7,
          "name": "Add & Search Words",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Design Add and Search Words Data Structure (LC 211) cold: addWord (identical to trie insert) plus a recursive search that, on a '.', tries every child, and on a normal letter follows that one child only, checking the word-lamp when the query is exhausted.",
          "teaches": "Writing DFS over trie children with a wildcard: carrying a node + index through the recursion, fanning out on '.', early-returning True on the first matching branch, and only checking the end-of-word lamp when the query runs out. Fuses the trie substrate with recursion \u2014 the last skill needed before the boss.",
          "engineLevelId": "tries/add-search-words",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "tries.l8",
          "order": 8,
          "name": "BOSS: Word Search II",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "Word Search II (LC 212): given a grid of letter-crates and a word list, return every word traceable through adjacent cells. The level first shows the brute-force baseline (run a grid backtracking DFS once per word) browning out on many words, then you build the trie-guided solution: load all words into a trie and run a SINGLE backtracking DFS over the grid, using the trie to prune the instant no word continues down the current path.",
          "teaches": "Combining the trie with grid backtracking from the Backtracking chapter: one shared DFS drives all words at once, the current trie-node rides along with the search finger, visited crates are marked and restored on the way back, and matches are collected wherever a word-lamp lights. Shows how a trie collapses N independent searches into one pass and turns an exponential brute force into a solution that clears the power meter.",
          "engineLevelId": "tries/boss-word-search-ii",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "greedy",
      "actId": "search-optimize",
      "order": 4,
      "title": "Greedy: Commit and Never Look Back",
      "oneLineGoal": "Recognize when a single forward sweep that commits to the locally best move \u2014 and never backtracks \u2014 beats searching every option.",
      "mechanism": "At each step commit to the choice that looks best right now and never undo it, carrying one running \"best\"/\"frontier\" register through a single forward pass, trusting that locally optimal moves compound into the global optimum.",
      "prereqChapters": [
        "foundations",
        "arrays-hashing",
        "heaps"
      ],
      "bossCombinesWith": [
        "heaps"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "greedy.l1",
          "order": 1,
          "name": "The Committed Robot",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "Poke-and-watch (no fail): a hauler robot walks a tape of crates holding mixed +/- values, carrying a 'haul' battery. At each crate you poke to watch it make one choice \u2014 extend the current haul (add this crate) or dump it and restart from here \u2014 while a separate 'best haul ever' ledger quietly records the maximum. Watch the running total get dumped the instant carrying it would hurt. This is Maximum Subarray seen from zero, with no code to write.",
          "teaches": "The greedy contract itself: one forward pass, an irreversible local choice at each crate, and a single running-best register \u2014 you never walk backward or reconsider a crate.",
          "engineLevelId": "greedy/the-committed-robot",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "greedy.l2",
          "order": 2,
          "name": "Best Haul on the Tape",
          "kind": "tutorial",
          "difficulty": 2,
          "problemText": "Maximum Subarray (NeetCode). Watch the tracer run Kadane's algorithm on the crate tape: cur = max(x, cur + x); best = max(best, cur). Use the scrubber to see cur reset to the current crate's value the moment carrying the old prefix sum would drag it down.",
          "teaches": "Kadane's greedy rule \u2014 drop the running sum the instant it goes negative, since a negative prefix can only shrink any future haul; keep the global best in a separate register.",
          "engineLevelId": "greedy/best-haul-on-the-tape",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "greedy.l3",
          "order": 3,
          "name": "The Reach Frontier",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Jump Game (NeetCode). A robot must decide if it can reach the last crate, where each crate's number is its maximum jump length. Fill the one missing line that extends the reachable frontier: reach = max(reach, i + nums[i]); the surrounding code already bails if index i ever passes reach.",
          "teaches": "Feasibility greedy \u2014 keep only the single farthest index you can reach; as long as the current index sits inside reach, the frontier can always be pushed further, so no branching search is needed.",
          "engineLevelId": "greedy/the-reach-frontier",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "greedy.l4",
          "order": 4,
          "name": "Trim the Overshoot",
          "kind": "guided",
          "difficulty": 3,
          "problemText": "Merge Triplets to Form Target Triplet (NeetCode). Fill the guard line that discards any triplet exceeding the target in any position \u2014 if a <= t[0] and b <= t[1] and c <= t[2] \u2014 so that only harmless triplets get OR-max'd position-by-position into the target.",
          "teaches": "A second greedy flavor, filter-then-accumulate: throw away every candidate that could never help (it overshoots somewhere), then safely combine what remains without any backtracking.",
          "engineLevelId": "greedy/trim-the-overshoot",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "greedy.l5",
          "order": 5,
          "name": "Clean Cuts",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Partition Labels (NeetCode). Write it cold: precompute each character's last index into a hash map, then sweep the string expanding the current segment's end via end = max(end, last[c]); cut a partition the moment the index reaches that end, then start the next segment fresh.",
          "teaches": "Greedy boundary-growing \u2014 grow a window only as far as the farthest obligation inside it forces you to, then commit the cut and never merge backward; the last-occurrence map turns a search into one sweep.",
          "engineLevelId": "greedy/clean-cuts",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "greedy.l6",
          "order": 6,
          "name": "Fewest Leaps",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Jump Game II (NeetCode). Write the minimum-jumps solver cold: track the end of the current jump's range and the farthest index reachable within it; when the index hits the range end, spend one jump and push the range out to farthest. Return the jump count.",
          "teaches": "Optimization greedy as level-by-level (BFS-by-range) expansion \u2014 you never choose which crate to land on; you only count a jump when the current reach is exhausted, always extending to the best frontier seen so far.",
          "engineLevelId": "greedy/fewest-leaps",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "greedy.l7",
          "order": 7,
          "name": "One Loop, No Retries",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Gas Station (NeetCode). The obvious 'try starting at every station' is O(n^2) and browns out the power meter on large tapes. Hit the O(n) target: if total gas >= total cost a solution exists, and the answer is the station right after wherever the running tank last dipped below zero.",
          "teaches": "Collapsing an O(n^2) search into one greedy pass \u2014 any prefix whose tank goes negative can never be a valid start, so reset the candidate start there instead of re-scanning, justified by a single global feasibility check.",
          "engineLevelId": "greedy/one-loop-no-retries",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "greedy.l8",
          "order": 8,
          "name": "Deal the Straights",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "Hand of Straights (NeetCode). Count every card into a hash map, then repeatedly pull the smallest available value off a min-heap and greedily build a run of groupSize consecutive cards, decrementing counts as you go; fail if any needed consecutive card is missing or its supply runs out. Fuses greedy commitment with a heap-driven 'always take the minimum' substrate feeding a Counter of supply.",
          "teaches": "Boss fusion of greedy + heaps \u2014 the smallest remaining card is forced to start a group, so a min-heap keeps handing you the one correct irreversible move while a Counter tracks remaining supply; combine the running-frontier discipline from earlier levels with a priority-queue as the decision engine.",
          "engineLevelId": "greedy/deal-the-straights",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "intervals",
      "actId": "search-optimize",
      "order": 5,
      "title": "Intervals: Sort, Sweep, and the Frontier",
      "oneLineGoal": "Kill the O(n^2) \"compare every pair\" instinct: sort intervals by a boundary, then sweep once to detect overlaps, merge bars, and count rooms.",
      "mechanism": "Sort intervals by one boundary, then sweep left-to-right exactly once, deciding every overlap and merge by comparing the current interval's edge to a running frontier (the furthest end seen so far).",
      "prereqChapters": [
        "foundations",
        "arrays-hashing",
        "greedy",
        "heaps"
      ],
      "bossCombinesWith": [
        "heaps"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "intervals.l1",
          "order": 1,
          "name": "The Overlap Lamp",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "Sandbox: two draggable interval bars sharing one number line. Drag their endpoints through presets (disjoint, touching at an endpoint, partial overlap, one nested inside the other); an 'overlap lamp' lights and highlights the shared region whenever they intersect. No code, no fail.",
          "teaches": "An interval is a [start, end] bar on a timeline; two bars overlap exactly when a.start <= b.end AND b.start <= a.end; the tricky endpoint-touch case ([1,2] vs [2,3]) and why < vs <= is the whole decision.",
          "engineLevelId": "intervals/the-overlap-lamp",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "intervals.l2",
          "order": 2,
          "name": "Sort, Then Sweep",
          "kind": "tutorial",
          "difficulty": 2,
          "problemText": "Meeting Rooms I \u2014 given meeting [start, end] intervals, return True if one person can attend them all (no two overlap). Watch the tracer: crates reorder as it sorts by start, then a robot sweeps left-to-right comparing each start against the previous meeting's end; the first collision flips the lamp to False.",
          "teaches": "Sorting by start collapses 'compare everyone to everyone' (O(n^2)) into a single O(n log n) sweep; once sorted, an overlap can only happen with the immediately previous interval, so you only ever compare curr.start to prev.end.",
          "engineLevelId": "intervals/sort-then-sweep",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "intervals.l3",
          "order": 3,
          "name": "Does It Fit?",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "Meeting Rooms I again, with the overlap test blanked out. Fill the single line inside the loop: the condition on intervals[i][0] and intervals[i-1][1] that returns False when two sorted meetings collide.",
          "teaches": "Writing the overlap comparison against the running previous end; choosing < vs <= so that back-to-back meetings ([1,2],[2,3]) are allowed while genuine overlaps are caught.",
          "engineLevelId": "intervals/does-it-fit",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "intervals.l4",
          "order": 4,
          "name": "The Frontier Robot",
          "kind": "tutorial",
          "difficulty": 3,
          "problemText": "Merge Intervals \u2014 merge all overlapping intervals into the fewest bars. Watch: after sorting by start, a 'current merged bar' grows rightward; when the next bar overlaps, the frontier end becomes max(frontier end, next end); when a gap appears, the finished bar is emitted to the output tape and a fresh one begins.",
          "teaches": "Carrying a running merged interval (the frontier) instead of comparing pairs; extend-with-max on the end (a later bar can be shorter, so you MUST take max, not just overwrite); emit-on-gap to close out a merged run.",
          "engineLevelId": "intervals/the-frontier-robot",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "intervals.l5",
          "order": 5,
          "name": "Collapse the Bars",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Merge Intervals \u2014 write it cold: sort by start, keep a result list, and for each interval either extend the last result bar (when it overlaps) or append a new one. Return the merged list.",
          "teaches": "Owning the full sort + sweep + merge loop end to end; the append-vs-extend branch; using result[-1] as the live frontier so you never need a separate tracking variable.",
          "engineLevelId": "intervals/collapse-the-bars",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "intervals.l6",
          "order": 6,
          "name": "Slot It In",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Insert Interval \u2014 given an already sorted, non-overlapping list plus one newInterval, return the list with newInterval inserted and merged. Write it as three passes: copy every interval that ends before newInterval starts, absorb every interval that overlaps newInterval (widening it), then copy the rest.",
          "teaches": "Exploiting input that is ALREADY sorted so no sort is needed (O(n), not O(n log n)); the three-region sweep (strictly-left / overlapping / strictly-right); merging a single moving bar by taking min of starts and max of ends.",
          "engineLevelId": "intervals/slot-it-in",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "intervals.l7",
          "order": 7,
          "name": "Fewest to Cut",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Non-overlapping Intervals \u2014 return the minimum number of intervals to remove so the rest don't overlap. A sort-by-start, keep-the-newest attempt browns out on adversarial inputs; hit the target by sorting on END and greedily keeping the interval that finishes earliest, dropping any whose start lands before that finish.",
          "teaches": "Greedy interval scheduling: sort by END (not start) so the earliest-finishing bar leaves the most room for later ones; each kept interval's end becomes the new frontier; a drop = a removal. Cements the judgment call of when to sort by start vs by end (builds on the Greedy chapter).",
          "engineLevelId": "intervals/fewest-to-cut",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "intervals.l8",
          "order": 8,
          "name": "How Many Rooms?",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "Meeting Rooms II \u2014 return the minimum number of rooms so no two meetings share one. Sort by start, sweep the meetings, and keep a min-heap of the end times of meetings still in progress: before each new meeting, pop every room whose end <= this start (freed up), then push this meeting's end; the peak heap size is the answer.",
          "teaches": "Fusing the interval sweep with a heap: the min-heap holds the frontier of all currently-open meetings, and its smallest end is the soonest a room frees; peak heap size = max concurrent overlaps = rooms needed. Combines the Intervals sweep with the Heaps pattern.",
          "engineLevelId": "intervals/how-many-rooms",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    },
    {
      "id": "dynamic-programming",
      "actId": "search-optimize",
      "order": 6,
      "title": "Dynamic Programming \u2014 The Memo Tape",
      "oneLineGoal": "Turn slow, self-repeating recursion into fast table-filling by spotting overlapping subproblems and caching each subproblem's answer exactly once.",
      "mechanism": "Many problems recompute the same subproblem exponentially often; DP stores each subproblem's answer once on a memo tape and builds bigger answers from already-solved smaller ones, turning brown-out exponential recursion into fast linear table-filling.",
      "prereqChapters": [
        "foundations",
        "recursion",
        "arrays-hashing",
        "binary-search"
      ],
      "bossCombinesWith": [
        "binary-search"
      ],
      "unlockRule": "allPrereqsCleared",
      "levelUnlockRule": "sequential",
      "levels": [
        {
          "id": "dynamic-programming.l1",
          "order": 1,
          "name": "The Recompute Trap",
          "kind": "primer",
          "difficulty": 1,
          "problemText": "Fibonacci (mechanism primer): poke a naive recursive fib(n) and watch the call-tree robots re-scan the same numbered crates over and over as the brown-out power meter spikes red; then flip the MEMO TAPE lever and watch every repeat call get answered instantly from a cache crate, the tree collapsing and the meter going green.",
          "teaches": "Overlapping subproblems: naive recursion redoes identical work exponentially, while a memo (cache) makes each subproblem cost once \u2014 the entire reason DP exists.",
          "engineLevelId": "dynamic-programming/the-recompute-trap",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "dynamic-programming.l2",
          "order": 2,
          "name": "Climb the Tape",
          "kind": "tutorial",
          "difficulty": 1,
          "problemText": "Climbing Stairs (LC 70): watch a bottom-up solution fill a tape of crates left to right, each crate dp[i] = dp[i-1] + dp[i-2], with two robots reading the two crates just behind them.",
          "teaches": "Writing a recurrence and filling a 1-D DP table bottom-up (tabulation), reading only from already-solved smaller crates instead of recursing.",
          "engineLevelId": "dynamic-programming/climb-the-tape",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "dynamic-programming.l3",
          "order": 3,
          "name": "Cheapest Steps",
          "kind": "tutorial",
          "difficulty": 2,
          "problemText": "Min Cost Climbing Stairs (LC 746): watch the tape fill where each crate takes the cheaper of its two predecessors plus that step's cost, dp[i] = cost[i] + min(dp[i-1], dp[i-2]).",
          "teaches": "A DP transition that chooses the best (min) among options and accumulates a cost \u2014 DP as picking the best predecessor, not just summing neighbors.",
          "engineLevelId": "dynamic-programming/cheapest-steps",
          "targetComplexity": null,
          "passRequires": "viewed"
        },
        {
          "id": "dynamic-programming.l4",
          "order": 4,
          "name": "Rob the Street",
          "kind": "guided",
          "difficulty": 2,
          "problemText": "House Robber (LC 198): the tape and robots are wired up except one crate's formula is blank \u2014 fill the transition dp[i] = max(dp[i-1], nums[i] + dp[i-2]) so the robber takes the best of skip-this-house versus rob-this-house-plus-two-back.",
          "teaches": "The take-or-skip decision: each cell is a max over 'use this element' vs 'don't', with the no-adjacent constraint encoded by reaching back to i-2.",
          "engineLevelId": "dynamic-programming/rob-the-street",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "dynamic-programming.l5",
          "order": 5,
          "name": "The Ring of Houses",
          "kind": "guided",
          "difficulty": 3,
          "problemText": "House Robber II (LC 213): houses sit on a circle so the first and last are now adjacent; fill the wiring that runs your House-Robber solver twice \u2014 once on houses[0..n-2], once on houses[1..n-1] \u2014 and returns the max of the two.",
          "teaches": "Reducing a new constraint (a circular street) to a problem you already solved by splitting it into two linear subproblems.",
          "engineLevelId": "dynamic-programming/the-ring-of-houses",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "dynamic-programming.l6",
          "order": 6,
          "name": "Break the Word",
          "kind": "open",
          "difficulty": 3,
          "problemText": "Word Break (LC 139): write it cold. Over a tape indexed by string position, dp[i] is reachable if some earlier reachable crate j has s[j:i] in the word set; return dp[len(s)].",
          "teaches": "Boolean reachability DP over a string, using an inner scan back to earlier crates plus O(1) hash-set membership for the dictionary lookup.",
          "engineLevelId": "dynamic-programming/break-the-word",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "dynamic-programming.l7",
          "order": 7,
          "name": "Make the Change",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Coin Change (LC 322): write it cold. Build a tape indexed by AMOUNT rather than by position; dp[a] = min over coins c of dp[a-c] + 1, holding unreachable amounts at an infinity sentinel and returning dp[amount] or -1.",
          "teaches": "DP over a value/amount axis with unbounded reuse of choices, minimizing a count, and using an infinity sentinel to mark impossible cells.",
          "engineLevelId": "dynamic-programming/make-the-change",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "dynamic-programming.l8",
          "order": 8,
          "name": "Decode Under Pressure",
          "kind": "optimize",
          "difficulty": 4,
          "problemText": "Decode Ways (LC 91): you're handed a correct but exponential recursion that branches on take-one-digit versus take-two-digits and browns out on a long digit string; add a memo (or convert to an O(n) tape) to clear the power meter \u2014 while handling '0', valid '10'/'20', and invalid '30'.",
          "teaches": "Recognizing overlapping subproblems in a fresh problem and applying the memo mechanism yourself to collapse exponential recursion to O(n) \u2014 the primer's lesson, now self-driven, with careful transition edge cases.",
          "engineLevelId": "dynamic-programming/decode-under-pressure",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        },
        {
          "id": "dynamic-programming.l9",
          "order": 9,
          "name": "The Longest Climb",
          "kind": "open",
          "difficulty": 4,
          "problemText": "Longest Increasing Subsequence (LC 300): write the O(n^2) DP cold \u2014 dp[i] = 1 + max(dp[j] for all j<i with nums[j] < nums[i]), and the answer is max(dp).",
          "teaches": "Subsequence DP where each crate depends on ALL earlier crates (not just fixed neighbors), scanning back to extend the best compatible increasing run.",
          "engineLevelId": "dynamic-programming/the-longest-climb",
          "targetComplexity": null,
          "passRequires": "correct"
        },
        {
          "id": "dynamic-programming.l10",
          "order": 10,
          "name": "Patience Under Brown-Out",
          "kind": "boss",
          "difficulty": 5,
          "problemText": "Longest Increasing Subsequence, O(n log n) (LC 300): the O(n^2) inner scan browns out on a huge array; maintain a 'tails' tape and, for each number, BINARY-SEARCH the first tail >= it and overwrite it (patience sorting), hitting O(n log n) to beat the tighter power meter.",
          "teaches": "Fusing DP thinking with binary search: replace the O(n) inner scan with a binary-searched tails array to solve the same problem from the open level, now optimally.",
          "engineLevelId": "dynamic-programming/patience-under-brown-out",
          "targetComplexity": null,
          "passRequires": "correct+withinBudget"
        }
      ]
    }
  ]
}